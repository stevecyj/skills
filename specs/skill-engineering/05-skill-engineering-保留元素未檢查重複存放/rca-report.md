# 根因報告：skill-engineering－保留元素未檢查重複存放

- 紀錄目錄：`specs/skill-engineering/05-skill-engineering-保留元素未檢查重複存放/`
- 問題證據：使用者備份的提示詞（另一次對話中，用其他模型以 `skill-engineering` 優化 `plan-with-class-diagram` 後，步驟內文在載入 RuleFile 後仍重述規則；原始對話紀錄已無法取得）；本次對話中使用者的控制平面標準；上一輪紀錄 `specs/skill-engineering/03-plan-with-class-diagram-類別圖未標註新增修改刪除/blueprint.md`；本次的 `04-…/rca-report.md` 與 `06-…/rca-report.md`；`skill-engineering` 的 `SKILL.md` 與 5 份 RuleFile
- 確認紀錄：2026-09-25 使用者回覆「全部同意，繼續進行重新設計」，確認 R1、R2 與 H4 本次不處理；其後使用者改以控制平面為標準，回覆「好，執行」，確認 `skill-engineering` 設計階段的檢查，以及自身 SOP 的 6 個重述步驟一併處理

## 1. 使用者回報的問題

`skill-engineering` 優化 Skill 後，目標 Skill 仍留有重複存放的判準：步驟在讀取資源後重述資源的內容，RuleFile 也重述樣板或其他 Rule 已有的內容。使用者要求 `skill-engineering` 經手的任何 Skill 都消除這類重複，包括它自己。

## 2. 驗收條件

| 編號 | 驗收條件 | 來源 | 適用範圍 | 驗收情境 |
| ---- | -------- | ---- | -------- | -------- |
| E1 | 優化模式的工程藍圖中，每個標記保留或修改的元素，其表達的約束在 To-Be 中沒有第二個存放位置；表達同一約束的其他元素標記為刪除或修改，理由指出保留的位置。即使本次問題與重複無關，也適用 | 本次 | 通用 | S1 |
| E2 | 根因分析遇到「同一約束存放在兩處以上」時，可以直接歸入缺陷類型表中的一種，不需要自行延伸定義 | 本次 | 通用 | S2 |
| E3 | `skill-engineering` 自己的 SOP 步驟只包含控制平面，不重述自己的 RuleFile、樣板組或腳本已規範的內容；它自己的 RuleFile 之間、RuleFile 與樣板組之間，也沒有重複存放的判準 | 本次 | 通用 | 施工後以 `skill-form-sop` 的完成檢查清單逐步檢查 |
| E4 | 追溯結果相同且沒有重複存放的既有內容，在 To-Be 中保留原文 | 既有 | 通用 | S1、S2 |
| E5 | 使用者明確確認根因報告與工程藍圖之前，不修改任何 Skill 檔案 | 既有 | 通用 | S1、S2 |
| E6 | 來源為推定或適用範圍無法判斷的驗收條件，在根因報告與工程藍圖的確認閘門中逐條請使用者確認 | 既有 | 通用 | S1、S2 |
| E7 | 施工差異發生時停止施工並記錄差異，返回重新設計並取得確認，重新施工時只執行「待施工」的工作項 | 既有 | 通用 | S3 |
| E8 | 本次修改不新增 `skill-engineering` 的 SOP 步驟，SOP 步驟內文總長度不增加 | 本次 | 個案 | 不適用，只檢查本次施工 |

## 3. 落差

| 驗收條件 | 預期 | 實際 | 證據 |
| -------- | ---- | ---- | ---- |
| E1 | 上一輪藍圖把重複存放的既有元素標記為刪除或修改 | `類別圖輸出` Rule 1、3、4 的條列以 E7 為依據標記保留；P3-1～P3-4 以 E9 為依據標記保留。但同一份藍圖的 C9 已把提案結構分配給樣板組，P3-2 也重述 `類別設計` Rule 4 | 觀察：03 藍圖「保留」表；約束分配表 C9；04 根因報告 R4、R5 |
| E1 | 重複存放的元素已移除，修改一處時不需要同步修改另一處 | 03 第二回合修改樣板（W6）時，必須同時改寫 Rule 1 第 2 條（W5） | 觀察：03 藍圖第 7-1 節 |
| E2 | 重複存放有對應的缺陷類型 | 04 根因報告必須另外說明延伸解釋，才能歸入「衝突」 | 觀察：04 根因報告第 5 節開頭 |
| E3 | 步驟只包含控制平面 | P1-4 重述 `next_record_dir.py` 的編號規則；P1-11 重述 `驗收條件` Rule 2 第 2 條；P2-8 的結束條件重述 `根因分析` Rule 5 第 1 條；P3-2 重述 `重新設計` Rule 1 第 1 條；P4-1 重述 `驗收條件` Rule 2 第 4 條與 Rule 3 第 2 條；P5-4 重述 `施工委派` Rule 5 第 1、2 條 | 觀察：各步驟內文與對應條列 |
| E3 | 資源之間沒有重複存放 | `根因分析` Rule 6 第 1 條重述根因報告骨架的段落；Rule 6 第 4 條的推定部分重述 `驗收條件` Rule 2、3；`施工委派` Rule 5 第 3 條把 Phase 轉移寫進 RuleFile，與 P5-4 的流程重複 | 觀察：各條列、骨架與 P5-4 |

## 4. 演練表

`skill-engineering`（重播 03 紀錄）：

| 步驟 | 應載入 | 實際載入 | 應產出 | 實際產出 | 判定 |
| ---- | ------ | -------- | ------ | -------- | ---- |
| P1-10～P1-12 | 驗收條件檢查清單 | 推論：已載入（03 藍圖的驗收條件都有來源與適用範圍欄） | 驗收條件 | 觀察：E6 定義提案結構，E7 定義一致性 | 一致 |
| P3-1 | 設計檢查清單 | 推論：已載入（約束分配表都附有依強制力順序說明的理由） | 設計檢查清單 | 觀察：已建立 | 一致 |
| P3-2、P3-3 | 設計檢查清單 | 觀察：已使用 | To-Be 與約束的存放位置 | 觀察：C9 只分配給樣板組 | 一致 |
| P3-4 | 設計檢查清單 | 觀察：已使用 | 差異表；只有唯一存放位置的元素可以保留 | 觀察：保留了約束與 C9 重複的 Rule 1 條列，理由只寫「E7：Mermaid 結構」；保留了重述實作順序推導的 P3-2，理由只寫「E9」 | 偏離 |
| P3-7 | 設計檢查清單 | 觀察：已使用 | 逐條驗證結果 | 推論：已通過；`重新設計` Rule 2 只要求保留元素「寫出追溯」，這兩列都有追溯，所以判定通過 | 一致 |
| P5-3（W1 `skill-form-sop`） | 工作項內容 | 觀察：To-Be SOP | 依已確認設計寫入 | 觀察：沒有攔下重述；原因見 06 根因報告 R1、R2 | 一致（下游問題另案處理） |

`skill-engineering`（本次 04 根因分析）：

| 步驟 | 應載入 | 實際載入 | 應產出 | 實際產出 | 判定 |
| ---- | ------ | -------- | ------ | -------- | ---- |
| P2-4 | 根因分析檢查清單 | 觀察：已載入 | 根因歸入一種缺陷類型 | 觀察：「衝突」的判斷方式只寫「互相矛盾」，重複存放沒有對應類型 | 偏離 |

第一個偏離點：03 紀錄的 P3-4。04 紀錄的 P2-4 是另一個獨立的偏離點。E3 的落差來自 `skill-engineering` 自己的 SOP 經由 `skill-form-sop` 寫入，根因與 06 的 R1、R2 相同，本報告不重複列出。

## 5. 根因

| 編號 | 位置 | 缺陷類型 | 說明與證據 | 反事實檢驗 |
| ---- | ---- | -------- | ---------- | ---------- |
| R1 | `skill-engineering`／`rules/重新設計-格式規範.md`／Rule 2 | 缺失 | 觀察：Rule 2 的條列只要求保留或修改的元素「寫出追溯」，沒有要求元素所表達的約束在 To-Be 中只存放在一處。「已由其他位置保證，因此無法追溯」只出現在 Good Example 第 1 列 | 假設 Rule 2 要求「約束另有存放位置時視為無法追溯，只保留一處」：P3-4 會把 Rule 1 第 1–3 條對照到 C9 而標記刪除，把 P3-2 對照到 `類別設計` Rule 4 而標記修改；P3-7 逐條驗證時可以檢查出來。這個判斷在設計階段進行，與施工途徑無關 |
| R2 | `skill-engineering`／`rules/根因分析-格式規範.md`／Rule 3 缺陷類型表「衝突」 | 缺失 | 觀察：判斷方式只涵蓋互相矛盾，以及範例與規則說明不一致 | 假設判斷方式加入「同一約束存放在兩處以上」：04 的 P2-4 可以直接歸類，修改方向「只保留一個來源」也直接對應 |

## 6. 被排除的假設

| 假設 | 缺陷類型 | 排除依據 |
| ---- | -------- | -------- |
| H3：`約束分配` Rule 1 只套用在約束分配表的列上，檢查範圍不清 | 不可檢查 | 觀察：03 約束分配表的 C9 已經只指定樣板組一個存放位置；重複的元素是在差異表的「保留」判斷中被留下的，依據的是 `重新設計` Rule 2 |
| H4：`skill-derive-*` 抽取後沒有刪除胖 SOP 中的原內文 | 交接缺陷 | 觀察：03 紀錄的工作項沒有任何 `skill-derive-*`；使用者備份提示詞描述的事件缺少對話紀錄，無法判定。06 讓 `skill-form-sop` 在寫入 SOP 時攔截重述，`skill-derive-*` 也委派它寫入 SOP，所以即使出現抽取殘留也會被攔下，06 的回歸情境會驗證這條路徑 |
| H6：使用者個案偏好 | 非 Skill 問題 | 觀察：使用者要求 `skill-engineering`「所到之處」都要消除重複 |

## 7. 影響範圍

無：結構診斷顯示沒有其他 Skill 委派 `skill-engineering`。R1、R2 不改變它傳給 `skill-form-*`、`skill-derive-*` 的委派內容。

## 8. 待確認事項

1. 第 2 節的驗收條件是否正確、完整？E6、E7 是既有預期，加入是為了確保 E3 刪除重述後，原本的功能不受影響；E8 為個案。
2. 是否同意第 5 節的根因 R1、R2？

## 9. 第二輪根因分析（05-S1 驗收失敗）

- 問題證據：05-S1、05-S2 子代理產出的工程藍圖（暫存複本 `scratchpad/accept/05-S1/specs/skill-engineering/04-plan-with-class-diagram-提案缺少風險與待決問題/blueprint.md`、`scratchpad/accept/05-S2/specs/skill-engineering/04-plan-with-class-diagram-實作順序重複推導/blueprint.md`）

### 落差

| 驗收條件 | 預期 | 實際 | 證據 |
| -------- | ---- | ---- | ---- |
| E1 | 保留或修改的元素，其約束在 To-Be 中只有一個存放位置 | S1 保留整條 `類別圖輸出` Rule 1、2、6 與 P1-2～P1-4、Phase 3；S2 保留整條 Rule 1、6 與 P1-1～P1-5 | 觀察：兩份藍圖的「保留」表 |

### 演練表（S1）

| 步驟 | 應載入 | 實際載入 | 應產出 | 實際產出 | 判定 |
| ---- | ------ | -------- | ------ | -------- | ---- |
| P3-1 | 設計檢查清單（含新版 `重新設計` Rule 2） | 觀察：已讀取 | 設計檢查清單 | 觀察：已建立 | 一致 |
| P3-3 | 設計檢查清單 | 觀察：已使用 | 每條約束只有一個存放位置 | 觀察：約束分配表只列與本次需求相關的 C1–C5，以及兩條標為「不變」的既有約束；C7 的存放位置寫了「Rule 6 與樣板組的圖例」兩處 | 偏離 |
| P3-4 | 設計檢查清單 | 觀察：已使用 | 保留元素的約束沒有第二個存放位置 | 觀察：「保留」表以整條 Rule、整段步驟為單位，每列只寫追溯的驗收條件；C6 已分配給樣板組，Rule 1 仍被保留 | 偏離 |
| P3-7 | 設計檢查清單 | 觀察：已使用 | 找出重複存放 | 推論：每列都有追溯，檢查判定通過（依據 P3-4 的觀察） | 一致（檢查本身無法發現問題） |

第一個偏離點：P3-3。

### 根因

| 編號 | 位置 | 缺陷類型 | 說明與證據 | 反事實檢驗 |
| ---- | ---- | -------- | ---------- | ---------- |
| R3 | `skill-engineering`／`rules/約束分配-格式規範.md`／Rule 1 | 不可檢查 | 觀察：Rule 1 要求「每條約束只能有一個存放位置」，但沒有規定約束分配表要涵蓋哪些約束。S1、S2 都只列出與本次需求相關的約束；沒有列入表中的既有約束，永遠不會被檢查是否重複存放。C7 雖然寫了兩個存放位置，但因為標為「不變」而被放過。推論：新版 `重新設計` Rule 2 要求比對「另有存放位置」，但沒有完整的約束清單可以比對，執行者只能憑印象判斷，實際上就沒有比對（依據兩份藍圖的觀察） | 假設約束分配表必須涵蓋 To-Be 中每個步驟內文、RuleFile 條列、樣板組與腳本所表達的約束，且每個保留或修改的元素都要引用它的約束編號：Rule 1 第 1–3 條與骨架會引用同一個 C 編號，P1-2 與腳本會引用同一個 C 編號，C7 的兩個位置會被迫拆開或合併，P3-7 可以依「同一編號只能對應一個存放位置」機械檢查，E1 可滿足 |

### 被排除的假設

| 假設 | 缺陷類型 | 排除依據 |
| ---- | -------- | -------- |
| H7：新版 `重新設計` Rule 2 沒有被載入 | 未載入 | 觀察：S2 依該條列多刪除了 Rule 2 第 4 條，並在理由中引用「重新設計 Rule 2」 |
| H8：「保留」表的粒度要改成逐條列 | 不可檢查 | 推論：只改粒度，保留列仍然只寫追溯，沒有可比對的約束清單；而且保留表會和約束分配表重複列出同一批約束（依據 R3 的觀察） |
| H9：改由腳本偵測重複 | 載體過弱 | 推論：判斷兩段文字是否表達同一約束需要理解語意，`audit_skill.py` 只能比對結構與路徑 |

### 影響範圍

無：沒有其他 Skill 委派 `skill-engineering`。另外，RuleFile 之間、RuleFile 與樣板之間的重複，目前只有 `skill-engineering` 在設計階段檢查；`skill-form-rule` 寫入 RuleFile 時不會比對同一個 Skill 的其他部位，沒有像 06 那樣的第二道關卡。

### 補充根因（撰寫第二輪藍圖時發現）

| 編號 | 位置 | 缺陷類型 | 說明與證據 | 反事實檢驗 |
| ---- | ---- | -------- | ---------- | ---------- |
| R4 | `skill-engineering`／`templates/engineering-blueprint.example.md`／To-Be SOP、「保留」表與約束分配表 | 衝突 | 觀察：範例的「保留」表每列只寫追溯，約束分配表只列 3 條與本次相關的約束；範例的 To-Be SOP 本身就含有 P1-2「（預設 30）」、P1-4 重述腳本保證、P1-7 判準名詞清單與 P3-2 重新推導實作順序，與新版 `重新設計` Rule 2 和 06 的 `SOP-格式規範` Rule 4 矛盾。推論：P3-8 每次載入這份範例，執行者照著範例寫出「只寫追溯」的保留表（依據 S1、S2 兩份藍圖的觀察） | 假設範例的 To-Be SOP 不含重述、「保留」表每列列出約束編號、約束分配表涵蓋全部約束：範例與 R3 修正後的規則一致，P3-9 依範例寫出的保留表會帶出約束編號 |

### 確認紀錄（第二輪）

- 2026-09-25 使用者回覆「都依照建議」：同意 R3 與修改方向（約束分配表涵蓋全部約束，保留或修改的元素列出約束編號），並同意另外為 `skill-form-rule` 建立第二道關卡（07 紀錄）。R4 在同意之後才發現，於第二輪藍圖一併請使用者確認。

### 待確認事項

1. 是否同意 R3，以及修改方向：約束分配表涵蓋 To-Be 的全部約束（含不變的既有約束），保留或修改的元素引用約束編號？這會讓每份工程藍圖的約束分配表變長（`plan-with-class-diagram` 約 30–40 列）。
2. 是否另外為 `skill-form-rule` 建立第二道關卡（寫入 RuleFile 時讀取同一個 Skill 的其他部位，條列不得重述其他部位已規範的內容），比照 06 的 `skill-form-sop`？

## 10. 第三輪根因分析（05-S1' 部分通過）

### 落差

| 驗收條件 | 預期 | 實際 | 證據 |
| -------- | ---- | ---- | ---- |
| E1 | Rule 1 第 1–3 條（重述樣板骨架）標記刪除 | S1' 的 C10 把 Rule 1 全部條列打包成一條約束並分配給 Rule 1，第 1–3 條被保留 | 觀察：`scratchpad/accept2/05-S1r/.../blueprint.md` 約束分配表 C10 |

### 根因

| 編號 | 位置 | 缺陷類型 | 說明與證據 | 反事實檢驗 |
| ---- | ---- | -------- | ---------- | ---------- |
| R5 | `skill-engineering`／`rules/約束分配-格式規範.md`／Rule 1 | 不可檢查 | 觀察：Rule 1 沒有規定一條約束可以包含哪些要求。S1' 以整條 Rule 為一條約束（子代理自述「沒修改的既有 Rule 各以一條約束概括」），存放位置由其中需要判斷的要求決定，樣板已保證的要求被一起分到 RuleFile；S2' 把樣板結構（C9）與 `classDef`／語法檢查（C10）拆開，因此發現 D2。推論：同一套規則下，結果取決於執行者選擇的粒度（依據兩份藍圖的對照） | 假設 Rule 1 規定「存放位置判斷不同的要求必須拆成不同約束」：S1' 的 C10 必須拆成「code block 結構」（樣板組）與「`classDef`、語法檢查」（Rule 1），Rule 1 第 1–3 條會對應到樣板組的約束而被標記刪除 |

### 被排除的假設

| 假設 | 缺陷類型 | 排除依據 |
| ---- | -------- | -------- |
| H10：範例示範了打包粒度 | 衝突 | 觀察：工程藍圖範例的 C4–C10 以整條 Rule 為一列，但範例中每條 Rule 只有一個存放位置判斷相同的要求，沒有示範打包不同存放位置的要求；S2' 讀了同一份範例仍選擇拆開 |
| H11：D4、D5 也是本輪失敗 | 非 Skill 問題 | 觀察：兩個子代理對 D4、D5 的判斷不同（D4：S1' 保留、S2' 刪除；D5：兩者都判斷為以名稱引用圖例）。推論：「使用標記名稱」算引用還是重述屬於判斷空間，不是規則缺漏 |

### 待確認事項

1. 是否同意 R5，並在 `約束分配` Rule 1 加入「存放位置判斷不同的要求，必須拆成不同約束」，再重跑 S1？
2. D5（Rule 6 使用圖例中的標記名稱）是否接受「以名稱引用圖例」的判斷？04 已依原設計把 Rule 6 改為不列舉標記名稱。

### 確認紀錄（第三輪）

- 2026-09-25 使用者回覆「確認，開始執行」：同意 R5 與修改方向；接受 D5 屬於以名稱引用圖例，不列入 E1 的判斷。
