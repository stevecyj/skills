# 工程藍圖：skill-engineering－保留元素未檢查重複存放

- 模式：優化
- 紀錄目錄：`specs/skill-engineering/05-skill-engineering-保留元素未檢查重複存放/`
- 根因報告：同一紀錄目錄的 `rca-report.md`；使用者已確認 R1、R2、H4 本次不處理，以及以控制平面為標準處理自身的重述（「好，執行」）
- 施工差異：2026-09-25 施工 W1 時，新版 `skill-form-sop`（06 已施工）讀取 `skill-engineering` 的部位並依新 `SOP-格式規範` Rule 4 檢查 To-Be，回報 To-Be 仍有 2 處步驟重述部位：P5-3「傳入該工作項內容作為已確認的設計」重述 `施工委派` Rule 4 第 1 條；P2-4「依結構診斷的呼叫關係列出影響範圍」重述 `根因分析` Rule 6「影響範圍必須依呼叫關係…」。另外 Phase 5 完成條件「Skill 連結已依是否安裝 Claude Code 同步」重述 `sync_agent_links.py` 的判斷（`約束分配` Rule 1：完成條件不得重述判斷細節）。依 `施工委派` Rule 5 停止 05 的施工，W1–W4 維持「待施工」，返回 Phase 3。重新設計為 SE9–SE11（P2-4、P5-3、Phase 5 完成條件），2026-09-25 使用者回覆「確認」
- 施工差異（第二次）：2026-09-25 重新施工 W1 前，以新版 `SOP-格式規範` Rule 4 對 To-Be 全部 45 個步驟與 5 個完成條件再檢查一次，又發現 2 處重述：P1-4「不含空白的問題簡述」重述 `next_record_dir.py` 的檢查（含空白時以錯誤結束）；P5-9「部位中的範例與規則沒有互相矛盾」重述 `約束分配` Rule 1 第 4 條。停止施工，W1–W4 維持「待施工」，重新設計為 SE12、SE13，2026-09-25 使用者回覆「確認」
- 確認紀錄：2026-09-25 使用者於 Phase 4 回覆「開始施工」：同意全部刪除項目、施工工作項、施工順序（06 → 05 → 04），以及新增的既有驗收條件（04 E9；05 E6、E7；06 E2–E4）與個案驗收條件 05 E8

## 1. 驗收條件

| 編號 | 驗收條件 | 來源 | 適用範圍 |
| ---- | -------- | ---- | -------- |
| E1 | 優化模式的工程藍圖中，每個標記保留或修改的元素，其表達的約束在 To-Be 中沒有第二個存放位置；表達同一約束的其他元素標記為刪除或修改，理由指出保留的位置。即使本次問題與重複無關，也適用 | 本次 | 通用 |
| E2 | 根因分析遇到「同一約束存放在兩處以上」時，可以直接歸入缺陷類型表中的一種，不需要自行延伸定義 | 本次 | 通用 |
| E3 | `skill-engineering` 自己的 SOP 步驟只包含控制平面，不重述自己的 RuleFile、樣板組或腳本已規範的內容；它自己的 RuleFile 之間、RuleFile 與樣板組之間，也沒有重複存放的判準 | 本次 | 通用 |
| E4 | 追溯結果相同且沒有重複存放的既有內容，在 To-Be 中保留原文 | 既有 | 通用 |
| E5 | 使用者明確確認根因報告與工程藍圖之前，不修改任何 Skill 檔案 | 既有 | 通用 |
| E6 | 來源為推定或適用範圍無法判斷的驗收條件，在根因報告與工程藍圖的確認閘門中逐條請使用者確認 | 既有 | 通用 |
| E7 | 施工差異發生時停止施工並記錄差異，返回重新設計並取得確認，重新施工時只執行「待施工」的工作項 | 既有 | 通用 |
| E8 | 本次修改不新增 `skill-engineering` 的 SOP 步驟，SOP 步驟內文總長度不增加 | 本次 | 個案 |

## 2. To-Be SOP

````markdown
---
name: skill-engineering
description: 建立新 Skill，或依使用者回報的不滿意結果優化既有 Skill（包含透過上下游委派一起產出結果的相關 Skill）時，必須使用此 Skill；使用者提到建立 skill、create skill、改寫或優化 skill、skill 結果不如預期時使用。先定義驗收條件；優化時先做根因分析並取得使用者確認，再從預期重新設計、刪除無法追溯的流程與規則，並編排 skill-form-* 與 skill-derive-* 施工及實跑驗收。只為單一 SOP 步驟抽取單一部位時，改用對應的 skill-derive-*；只撰寫單一部位檔案時，改用對應的 skill-form-*。
---

# SOP

## Phase 1 -- 蒐證與定義預期

1. `READ` 使用者需求。
2. `THINK` 判定模式為新建或優化，並確定目標 Skill 名稱、本專案的所有 skills 目錄（例如 `.agents/skills`、`.claude/skills`、`skills`）、目標 Skill 所在的 skills 目錄與專案根目錄；新建模式的目標 Skill 一律位於專案的 `.agents/skills/<目標 Skill 名稱>/`，不論執行的 agent 為何。
3. `READ` 新建模式時，讀取所有 skills 目錄中其他 Skill 的 `name` 與 `description`；已有 Skill 涵蓋需求時，向使用者說明並改以優化模式處理該 Skill。
4. `DELEGATE` 本次紀錄目錄尚未確定時，在本 Skill 目錄執行 `uv run --script scripts/next_record_dir.py --project-root <專案根目錄絕對路徑> --skill <目標 Skill 名稱> --topic <主題>`，從標準輸出 JSON 的 `record_dir` 取得「本次紀錄目錄」；`<主題>` 在新建模式為「建立」，在優化模式為問題簡述。若缺少 `uv`，依 https://docs.astral.sh/uv/getting-started/installation/ 的目前平台安裝說明安裝後重試，安裝失敗時回報原因並停止。
5. `CHECK` 執行腳本時確認退出狀態為零且標準輸出為可解析的 JSON；失敗時依標準錯誤輸出修正參數後重試，仍失敗時回報原因並停止。
6. `READ` 優化模式時，讀取問題發生時的對話紀錄（本次對話上下文或使用者指定的紀錄檔）與不滿意的產出物，建立「問題證據」；缺少判斷所需的關鍵證據時，請使用者提供。
7. `DELEGATE` 優化模式時，在本 Skill 目錄執行 `uv run --script scripts/audit_skill.py --skills-root <skills 目錄絕對路徑> --skill <目標 Skill 名稱> --graph`，每個 skills 目錄各加一個 `--skills-root` 參數，從標準輸出取得 JSON「結構診斷」；若缺少 `uv`，依 https://docs.astral.sh/uv/getting-started/installation/ 的目前平台安裝說明安裝後重試，安裝失敗時回報原因並停止。
8. `CHECK` 執行腳本時確認退出狀態為零且標準輸出為可解析的 JSON；失敗時依標準錯誤輸出修正參數後重試，仍失敗時回報原因並停止。
9. `READ` 優化模式時，依「問題證據」與「結構診斷」的呼叫關係，讀取產出問題結果之 Skill 及其上下游 Skill 的 `SKILL.md`、所有 RuleFile 與樣板組，並以 `--help` 與檔頭說明了解每個腳本的介面，腳本與問題相關時才讀取全文，建立「產出鏈現況」。
10. `READ` 定義預期時，讀取 `rules/驗收條件-格式規範.md` 的所有 Rule，建立驗收條件檢查清單。
11. `THINK` 依已載入的驗收條件檢查清單，將使用者需求（優化模式另含「產出鏈現況」）寫成驗收條件與驗收情境。
12. `CHECK` 逐條驗證驗收條件檢查清單；未通過時修正驗收條件與驗收情境。

完成條件：已判定模式與本次紀錄目錄，驗收條件與驗收情境均通過驗收條件檢查清單；優化模式另已取得問題證據、結構診斷與產出鏈現況。

## Phase 2 -- 根因分析與確認

1. `CHECK` 新建模式時略過本 Phase，進入 Phase 3。
2. `READ` 分析根因時，讀取 `rules/根因分析-格式規範.md` 的所有 Rule，建立根因分析檢查清單。
3. `THINK` 依已載入的根因分析檢查清單，以「問題證據」沿「產出鏈現況」演練 SOP，找出第一個偏離點。
4. `THINK` 依根因分析檢查清單確定根因與缺陷類型，並依「結構診斷」列出影響範圍。
5. `CHECK` 逐條驗證根因分析檢查清單；未通過時補充證據或修正分析。
6. `READ` 撰寫根因報告時，讀取 `templates/rca-report.md` 與 `templates/rca-report.example.md`，建立根因報告樣板組。
7. `WRITE` 依已載入的根因報告樣板組與驗收條件檢查清單，將根因報告寫入本次紀錄目錄的 `rca-report.md`，並向使用者展示。
8. `CHECK` 使用者回覆：對驗收條件有異議，或補充內容改變了驗收條件時，返回 Phase 1；對根因有異議時，依回覆補充證據並重新執行本 Phase；確認且補充內容不改變驗收條件與根因時，將補充記入根因報告並視為確認；未明確確認時停止執行。使用者確認根因報告中不修改 Skill 的結論時，回報結論後結束，不進入 Phase 3。

完成條件：新建模式已略過本 Phase；優化模式的使用者已明確確認根因報告中的驗收條件與根因，且根因位於 Skill 內。

## Phase 3 -- 從預期重新設計

1. `READ` 設計 To-Be 時，讀取 `rules/重新設計-格式規範.md` 與 `rules/約束分配-格式規範.md` 的所有 Rule，建立設計檢查清單。
2. `THINK` 依已載入的設計檢查清單，推導 To-Be 的 frontmatter、Phase、步驟與每個步驟的約束。
3. `THINK` 依設計檢查清單為每條約束指定存放位置。
4. `THINK` 依設計檢查清單，將 To-Be 與「產出鏈現況」中的目標 Skill（新建模式為空）對照，產生差異表、淨增減與回歸情境。
5. `READ` 規劃施工時，讀取 `rules/施工委派-格式規範.md` 的所有 Rule，建立施工委派檢查清單。
6. `THINK` 依已載入的施工委派檢查清單，將差異整理成施工工作項與工作項內容，每個工作項的狀態設為「待施工」。
7. `CHECK` 逐條驗證設計檢查清單與施工委派檢查清單；未通過時修正設計與施工工作項。
8. `READ` 撰寫工程藍圖時，讀取 `templates/engineering-blueprint.md` 與 `templates/engineering-blueprint.example.md`，建立工程藍圖樣板組。
9. `WRITE` 依已載入的工程藍圖樣板組，將工程藍圖寫入本次紀錄目錄的 `blueprint.md`，驗收結果段落暫寫「尚未施工」。

完成條件：設計檢查清單與施工委派檢查清單均已通過，工程藍圖已寫入本次紀錄目錄。

## Phase 4 -- 確認藍圖

1. `WRITE` 依工程藍圖的段落順序與驗收條件檢查清單向使用者展示工程藍圖，並標出所有刪除項目；新建模式的驗收條件在此首次取得確認。
2. `CHECK` 使用者回覆；對驗收條件有異議時返回 Phase 1，要求修改其他內容時返回 Phase 3，未明確確認時停止執行；確認時將確認內容與推定驗收條件的回覆寫入工程藍圖的確認紀錄。

完成條件：使用者已明確確認目前版本的工程藍圖。

## Phase 5 -- 施工與驗收

1. `READ` 已確認的工程藍圖中各施工工作項的狀態，建立「待施工清單」。
2. `WRITE` 依施工委派檢查清單，刪除「待施工清單」中標記刪除的整個部位檔案，並在工程藍圖將這些工作項標記為「已完成」。
3. `DELEGATE` 依施工委派檢查清單，將「待施工清單」中其餘工作項逐項交給指定的 `skill-form-*` 或 `skill-derive-*`，每完成一項即在工程藍圖將其標記為「已完成」。
4. `CHECK` 「待施工清單」的工作項均已標記為「已完成」；依施工委派檢查清單因施工差異停止時，返回 Phase 3。
5. `DELEGATE` 在本 Skill 目錄執行 `uv run --script scripts/audit_skill.py --skills-root <skills 目錄絕對路徑> --skill <已修改的 Skill 名稱>`，每個 skills 目錄與每個已修改的 Skill 各加一個參數，取得「施工後結構診斷」；若缺少 `uv`，依 https://docs.astral.sh/uv/getting-started/installation/ 的目前平台安裝說明安裝後重試，安裝失敗時回報原因並停止。
6. `CHECK` 退出狀態為零，且「施工後結構診斷」沒有任何 `error`；有 `error` 時依施工委派檢查清單修正後重新執行上一步。
7. `DELEGATE` 在本 Skill 目錄執行 `uv run --script scripts/sync_agent_links.py --project-root <專案根目錄絕對路徑>`，從標準輸出取得 JSON「連結同步結果」；若缺少 `uv`，依 https://docs.astral.sh/uv/getting-started/installation/ 的目前平台安裝說明安裝後重試，安裝失敗時回報原因並停止。
8. `CHECK` 退出狀態為零且「連結同步結果」為可解析的 JSON；失敗時依標準錯誤輸出修正後重新執行上一步，仍失敗時回報原因並停止。
9. `CHECK` 依設計檢查清單，確認目標 Skill 的 frontmatter、SOP 與部位均與工程藍圖的 To-Be 及約束分配表一致。
10. `WRITE` 為每個驗收情境建立包含本次未提交修改的暫存專案複本，依情境描述準備測試環境，並記錄執行前的狀態（例如 `git status` 與相關檔案的雜湊值），以便比對副作用。
11. `CHECK` 每個測試環境均與其情境描述一致；不一致時修正測試環境。
12. `DELEGATE` 以不帶本次分析脈絡的子代理，只提供修改後 Skill 的路徑、測試環境位置、情境輸入與預先回覆，並要求回報每個確認閘門展示的內容與最終產出，逐一執行驗收情境，取得「驗收產出」；無法隔離副作用，或只驗證未受本次修改影響之步驟的情境，改為逐步演練並在驗收結果中註明。
13. `CHECK` 依驗收情境的判斷方式檢查「驗收產出」；情境失敗來自測試環境與情境描述不一致時，修正測試環境、記錄於驗收結果後重新執行該情境；失敗來自 Skill 時，優化模式返回 Phase 2 修正根因，新建模式返回 Phase 3 修正設計。
14. `WRITE` 將施工後結構診斷摘要、「連結同步結果」中的並存、衝突與移轉建議、各驗收情境的結果與驗收方式寫入工程藍圖的驗收結果段落，並向使用者回報結果與個案驗收條件的調整建議。

完成條件：工程藍圖的所有施工工作項均為「已完成」，施工後結構診斷沒有 `error`，Skill 連結已同步，所有驗收情境均已通過，且驗收結果已寫入工程藍圖並回報使用者。
````

## 3. 差異表

依新的 `重新設計` Rule 2 與控制平面標準（06 的 `SOP-格式規範` Rule 4）逐一檢查 As-Is，`skill-engineering` 本身有 9 處重複存放：步驟重述資源 6 處（SE1–SE5 的步驟部分、SE8），資源之間重複 3 處（SE5 的規則部分、SE6、SE7）。依該 Rule「刪除無法追溯的元素時，不受根因位置限制」，這些重複一併處理（E3）。

### 刪除

| 元素 | 位置 | 理由 |
| ---- | ---- | ---- |
| 「根因報告必須包含驗收條件、落差、演練表、第一個偏離點、根因、被排除的假設與影響範圍」 | `rules/根因分析-格式規範.md` Rule 6 第 1 條 | SE7：報告的段落已由根因報告骨架固定（C9），只保留樣板組 |
| 「差異必須回到工程藍圖重新設計，並取得使用者確認後才能繼續施工」 | `rules/施工委派-格式規範.md` Rule 5 第 3 條 | SE5：屬於 Phase 轉移與使用者確認，依約束分配 Rule 2 應留在 SOP（P5-4 返回 Phase 3，再經 Phase 4 確認）（C8） |

### 修改、合併、改換存放位置與新增

| 元素 | 動作 | 內容 | 追溯 |
| ---- | ---- | ---- | ---- |
| `rules/重新設計-格式規範.md` Rule 2 第 3 條 | 修改 | 「無法追溯的元素必須標記刪除」擴充為：約束在 To-Be 中另有存放位置時也視為無法追溯，只能保留一處，其餘元素標記刪除或修改，理由寫出保留的位置 | R1、E1（C1） |
| 同檔 Rule 2 正反範例 | 修改 | Good Example 加入「可追溯但重複的步驟補充標記修改」一列並調整說明；Bad Example 加入同一元素被保留的一列並調整說明 | R1、E1；範例不得與條列矛盾 |
| `rules/根因分析-格式規範.md` Rule 3 缺陷類型表「衝突」 | 修改 | 判斷方式改為「兩個部位的要求互相矛盾、同一約束存放在兩處以上，或範例與規則說明不一致」 | R2、E2（C2） |
| `rules/根因分析-格式規範.md` Rule 6 第 4 條 | 修改 | 刪除「逐條列出來源為推定或適用範圍無法判斷的驗收條件」，這項約束只保留在 `驗收條件` Rule 2、3 | SE6、E3、E6（C6） |
| P1-4 | 修改 | 刪除「（編號為既有最大編號加一，不回補缺號）」 | SE1、E3：由 `scripts/next_record_dir.py` 保證（C3） |
| P1-11 | 修改 | 「優化模式另從產出鏈現況推導既有核心預期」改為只標出輸入「（優化模式另含「產出鏈現況」）」 | SE2、E3：推導方式由 `驗收條件` Rule 2 規範（C4） |
| P2-7 | 修改 | 改為依「根因報告樣板組與驗收條件檢查清單」撰寫 | SE6、E6：推定條件的確認方式改由驗收條件檢查清單提供（C6） |
| P2-8 | 修改 | 結束分支改為「使用者確認根因報告中不修改 Skill 的結論時，回報結論後結束」 | SE8、E3：結論的成立條件與內容由 `根因分析` Rule 5 規範（C10） |
| P1-4（再次） | 修改 | 「不含空白的問題簡述」改為「問題簡述」（第二次施工差異） | SE12、E3：topic 不得含空白由 `scripts/next_record_dir.py` 檢查（C14） |
| P5-9 | 修改 | 改為「依設計檢查清單，確認…一致」，刪除「且部位中的範例與規則沒有互相矛盾」（第二次施工差異） | SE13、E3：範例不得與約束矛盾由 `約束分配` Rule 1 規範（C15） |
| P2-4 | 修改 | 「依「結構診斷」的呼叫關係列出影響範圍」改為「依「結構診斷」列出影響範圍」（施工差異） | SE9、E3：影響範圍的列法由 `根因分析` Rule 6 規範（C11） |
| P5-3 | 修改 | 刪除「傳入該工作項內容作為已確認的設計」，「依施工委派檢查清單的順序」改為「依施工委派檢查清單」（施工差異） | SE10、E3：委派內容與順序由 `施工委派` Rule 3、4 規範（C12） |
| Phase 5 完成條件 | 修改 | 「Skill 連結已依是否安裝 Claude Code 同步」改為「Skill 連結已同步」（施工差異） | SE11、E3：同步條件由 `scripts/sync_agent_links.py` 決定（C13） |
| P3-2 | 修改 | 刪除「從驗收條件（優化模式另含已確認的根因）」 | SE3、E3：推導來源由 `重新設計` Rule 1 規範（C5） |
| P4-1 | 修改 | 改為「依工程藍圖的段落順序與驗收條件檢查清單向使用者展示工程藍圖，並標出所有刪除項目」 | SE4、E3、E6（C6） |
| P5-4 | 修改 | 改為「依施工委派檢查清單因施工差異停止時，返回 Phase 3」 | SE5、E3、E7：記錄差異與只施工「待施工」由 `施工委派` Rule 5 規範（C7），返回 Phase 3 留在 SOP（C8） |

### 保留

| 元素 | 追溯 |
| ---- | ---- |
| frontmatter | E1–E5：description 已涵蓋優化既有 Skill；只概述結果，不重述判斷細節 |
| 其餘 38 個步驟與 5 個 Phase 的完成條件 | E3–E5、E7；逐步對照 5 個 RuleFile、2 組樣板與 3 個腳本，步驟內文只包含動作、輸入、命令、分支與只屬於該步驟的一句話約束（例如 P1-2 的新建位置、P3-6 的初始狀態、P4-1 的「所有刪除項目」）；各步驟重複出現的 `uv` 缺少處理屬於約束分配 Rule 1 允許的固定寫法 |
| `rules/重新設計-格式規範.md` Rule 1、3–6 | E1、E4；Rule 1 第 4 條是 E4 的唯一存放位置；Rule 3 中「衝突」的修改方向已涵蓋重複存放 |
| `rules/約束分配-格式規範.md` Rule 1–3 | E1；Rule 1 規範新分配的約束只能有一個存放位置，與 `重新設計` Rule 2 第 3 條（既有元素的保留判斷）約束的對象不同（H3） |
| `rules/根因分析-格式規範.md` Rule 1、2、4、5、Rule 3 其餘內容、Rule 6 第 2、3 條 | E2 |
| `rules/驗收條件-格式規範.md` Rule 1–4 | E6：Rule 2 第 4 條、Rule 3 第 2 條是推定與適用範圍確認方式的唯一存放位置 |
| `rules/施工委派-格式規範.md` Rule 1–4、Rule 5 第 1、2、4 條 | E7 |
| 根因報告樣板組、工程藍圖樣板組 | E1、E2；根因報告骨架是 C9 的唯一存放位置 |
| `scripts/audit_skill.py`、`next_record_dir.py`、`sync_agent_links.py` | E5；`next_record_dir.py` 是 C3 的唯一存放位置 |

## 4. 淨增減

| 項目 | As-Is | To-Be | 增減 |
| ---- | ----- | ----- | ---- |
| Phase | 5 | 5 | 0 |
| 步驟 | 45 | 45 | 0 |
| RuleFile | 5 | 5 | 0 |
| Rule | 24 | 24 | 0 |
| 樣板組 | 2 | 2 | 0 |
| 腳本 | 3 | 3 | 0 |

- 沒有新增任何條列：R1 改寫 `重新設計` Rule 2 第 3 條，R2 改寫缺陷類型表的一格。依 `重新設計` Rule 4 的順序考慮：沒有表達這兩項約束的既有內容可以刪除；改寫就能處理，因為「無法追溯就刪除」和「衝突」本來就是這兩項約束所屬的位置。
- 內容減少：刪除 2 條條列、縮短 1 條條列；SOP 10 個步驟縮短或改為引用，步驟內文總長度由 4390 字降為 4241 字（E8）。
- 範例：`重新設計` Rule 2 的正反範例各增加一列。

## 5. 約束分配表

| 編號 | 約束 | 所屬步驟 | 存放位置 | 理由 |
| ---- | ---- | -------- | -------- | ---- |
| C1 | 保留或修改的元素所表達的約束在 To-Be 中另有存放位置時，只保留一處，其餘元素標記刪除或修改，理由寫出保留的位置 | P3-4、P3-7 | `rules/重新設計-格式規範.md` Rule 2 | 需要判斷兩個元素是否表達同一約束，腳本無法可靠判斷語意；不是產出檔的整體結構，所以不放樣板組；持續適用且需要正反例；屬於既有元素保留判斷的主題，擴充既有條列 |
| C2 | 同一約束存放在兩處以上時歸入「衝突」 | P2-4 | `rules/根因分析-格式規範.md` Rule 3 | 屬於缺陷類型的判斷定義，改寫既有表格的一格 |
| C3 | 紀錄編號為既有最大編號加一，不回補缺號 | P1-4 | `scripts/next_record_dir.py` | 輸入輸出明確、可由程式判斷；腳本已經實作 |
| C4 | 優化模式從 description、完成條件與確認閘門推導既有核心預期 | P1-11 | `rules/驗收條件-格式規範.md` Rule 2 | 既有；需要判斷與正反例 |
| C5 | To-Be 從驗收條件與已確認的根因推導 | P3-2 | `rules/重新設計-格式規範.md` Rule 1 | 既有；需要判斷與正反例 |
| C6 | 推定或適用範圍無法判斷的驗收條件，在確認閘門逐條請使用者確認 | P2-7、P4-1 | `rules/驗收條件-格式規範.md` Rule 2、3 | 既有；兩種模式、兩個閘門共用一個來源 |
| C7 | 施工差異時停止並記錄；重新施工只執行「待施工」 | P5-4 | `rules/施工委派-格式規範.md` Rule 5 | 既有；需要判斷是否構成差異 |
| C8 | 施工差異返回重新設計，並經使用者確認後才能繼續 | P5-4 | SOP 流程 | 屬於 Phase 轉移與使用者確認 |
| C9 | 根因報告包含的段落 | P2-7 | 根因報告樣板組 | 約束的是產出檔的整體結構 |
| C10 | 不修改 Skill 的結論在什麼條件下成立，以及要包含哪些內容 | P2-4、P2-8 | `rules/根因分析-格式規範.md` Rule 5 | 既有；需要判斷與正反例 |
| C11 | 影響範圍依呼叫關係列出直接與間接呼叫者 | P2-4 | `rules/根因分析-格式規範.md` Rule 6 | 既有 |
| C12 | 施工順序，以及委派時傳入完整工作項內容並說明已經確認 | P5-3 | `rules/施工委派-格式規範.md` Rule 3、4 | 既有 |
| C13 | 是否建立 `.claude/skills` 連結取決於 Claude Code 是否安裝 | P5-7、Phase 5 完成條件 | `scripts/sync_agent_links.py` | 既有；可由程式判斷 |
| C14 | topic 不得含空白、路徑分隔字元或保留字元 | P1-4 | `scripts/next_record_dir.py` | 既有；可由程式判斷 |
| C15 | 範例不得與存放位置中的約束互相矛盾 | P5-9 | `rules/約束分配-格式規範.md` Rule 1 | 既有 |

## 6. 施工工作項

| 順序 | 工作項 | 執行者 | 目標 | 內容 | 約束 | 狀態 |
| ---- | ------ | ------ | ---- | ---- | ---- | ---- |
| 1 | W1 | `skill-form-sop` | `skill-engineering` 的 `SKILL.md` | 依 To-Be SOP 修改 10 個步驟與 Phase 5 完成條件 | C3–C8、C10–C15 | 已完成 |
| 2 | W2 | `skill-form-rule` | `rules/重新設計-格式規範.md` Rule 2 | 改寫第 3 條並更新正反範例 | C1 | 已完成 |
| 3 | W3 | `skill-form-rule` | `rules/根因分析-格式規範.md` Rule 3、Rule 6 | 改寫「衝突」判斷方式；刪除 Rule 6 第 1 條；縮短第 4 條 | C2、C6、C9 | 已完成 |
| 4 | W4 | `skill-form-rule` | `rules/施工委派-格式規範.md` Rule 5 | 刪除第 3 條 | C8 | 已完成 |

### 工作項內容

#### W1：SOP 修改

依第 2 節 To-Be SOP 修改下列步驟，其餘步驟保留原文：

| 步驟 | 修改後全文 |
| ---- | ---------- |
| Phase 1 -- 蒐證與定義預期中執行 `next_record_dir.py` 的 `DELEGATE` 步驟 | 刪除「（編號為既有最大編號加一，不回補缺號）」，其餘文字不變 |
| Phase 1 -- 蒐證與定義預期中寫成驗收條件的 `THINK` 步驟 | `THINK` 依已載入的驗收條件檢查清單，將使用者需求（優化模式另含「產出鏈現況」）寫成驗收條件與驗收情境。 |
| Phase 2 -- 根因分析與確認中寫入 `rca-report.md` 的 `WRITE` 步驟 | `WRITE` 依已載入的根因報告樣板組與驗收條件檢查清單，將根因報告寫入本次紀錄目錄的 `rca-report.md`，並向使用者展示。 |
| Phase 2 -- 根因分析與確認中檢查使用者回覆的 `CHECK` 步驟 | 最後一句改為「使用者確認根因報告中不修改 Skill 的結論時，回報結論後結束，不進入 Phase 3。」，其餘文字不變 |
| Phase 3 -- 從預期重新設計中推導 To-Be 的 `THINK` 步驟 | `THINK` 依已載入的設計檢查清單，推導 To-Be 的 frontmatter、Phase、步驟與每個步驟的約束。 |
| Phase 4 -- 確認藍圖中展示工程藍圖的 `WRITE` 步驟 | `WRITE` 依工程藍圖的段落順序與驗收條件檢查清單向使用者展示工程藍圖，並標出所有刪除項目；新建模式的驗收條件在此首次取得確認。 |
| Phase 1 -- 蒐證與定義預期中執行 `next_record_dir.py` 的 `DELEGATE` 步驟（再次） | 「在優化模式為不含空白的問題簡述」改為「在優化模式為問題簡述」 |
| Phase 5 -- 施工與驗收中比對 To-Be 的 `CHECK` 步驟 | `CHECK` 依設計檢查清單，確認目標 Skill 的 frontmatter、SOP 與部位均與工程藍圖的 To-Be 及約束分配表一致。 |
| Phase 2 -- 根因分析與確認中確定根因與缺陷類型的 `THINK` 步驟 | `THINK` 依根因分析檢查清單確定根因與缺陷類型，並依「結構診斷」列出影響範圍。 |
| Phase 5 -- 施工與驗收中逐項委派工作項的 `DELEGATE` 步驟 | `DELEGATE` 依施工委派檢查清單，將「待施工清單」中其餘工作項逐項交給指定的 `skill-form-*` 或 `skill-derive-*`，每完成一項即在工程藍圖將其標記為「已完成」。 |
| Phase 5 的完成條件 | 「Skill 連結已依是否安裝 Claude Code 同步」改為「Skill 連結已同步」，其餘文字不變 |
| Phase 5 -- 施工與驗收中檢查「待施工清單」的 `CHECK` 步驟 | `CHECK` 「待施工清單」的工作項均已標記為「已完成」；依施工委派檢查清單因施工差異停止時，返回 Phase 3。 |

#### W2：`rules/重新設計-格式規範.md` Rule 2

- 第 3 條改為：「無法追溯的元素必須標記刪除；元素所表達的約束在 To-Be 中另有存放位置時也視為無法追溯，只能保留一處，其餘元素標記刪除或修改，理由寫出保留的位置。」
- Good Example 說明改為：「每個元素都有追溯依據；無法追溯的重複描述標記刪除，可追溯但約束另有存放位置的步驟補充標記修改。」表格最後加入一列：`| P1-7 載入類別設計檢查清單後補充的「測試類別不列入設計」 | 修改 | 可追溯至 E7，但同一約束已存放在 `rules/類別圖輸出-格式規範.md` Rule 2，只保留該處；P1-7 刪除這段補充，其餘文字保留原文 |`
- Bad Example 說明改為：「保留理由無法追溯到任何驗收條件或必要流程；步驟補充雖可追溯，但約束已存放在 RuleFile，仍被保留。」表格最後加入一列：`| P1-7 載入類別設計檢查清單後補充的「測試類別不列入設計」 | 保留 | E7：測試類別不出現在類別圖中 |`

完成後全文應與同目錄下的 `W2-重新設計-格式規範.md` 逐字一致。

#### W3：`rules/根因分析-格式規範.md`

- Rule 3 缺陷類型表「衝突」列的判斷方式改為：「兩個部位的要求互相矛盾、同一約束存放在兩處以上，或範例與規則說明不一致」。
- Rule 6 刪除第 1 條「根因報告必須包含驗收條件、落差、演練表、第一個偏離點、根因、被排除的假設與影響範圍。」
- Rule 6 第 4 條改為：「待確認事項必須包含驗收條件是否正確完整、是否同意根因，以及仍缺少的證據。」範例不變（Good Example 詢問推定的 E7，與 `驗收條件` Rule 2 一致）。

完成後全文應與同目錄下的 `W3-根因分析-格式規範.md` 逐字一致。

#### W4：`rules/施工委派-格式規範.md` Rule 5

- 刪除第 3 條「差異必須回到工程藍圖重新設計，並取得使用者確認後才能繼續施工。」範例不變。

完成後全文應與同目錄下的 `W4-施工委派-格式規範.md` 逐字一致。

## 7. 驗收情境

兩個情境都在暫存專案複本中執行：`skill-engineering` 使用本次修改後的版本；`plan-with-class-diagram` 還原為 HEAD 版本（仍含 04 紀錄的 D1–D5 重複）；刪除 04、05 紀錄目錄，避免答案外洩。

| 編號 | 驗證 | 類型 | 輸入 | 預先回覆 | 判斷方式 |
| ---- | ---- | ---- | ---- | -------- | -------- |
| S1 | E1、E4、E5、E6 | 本次 | 以 `skill-engineering` 優化 `plan-with-class-diagram`：使用者看了 `specs/plan/03-request-logging/class-diagram-proposal.md` 後，發現提案沒有列出仍待決定的設計問題（例如 `LoggingHandler` 的 Logger 注入點是否公開），希望每份提案都有「風險與待決問題」段落 | Phase 2 回覆「確認」；Phase 4 回覆「先不要施工」 | E1：藍圖的「保留」表不能同時保留 D1–D5 任一組重複的兩端（`類別圖輸出` Rule 1 第 1–3 條對樣板骨架；Rule 2 第 4 條對 Rule 5 第 2 條；Rule 5 第 3 條對 `類別設計` Rule 4；Rule 6 標記列舉對圖例；P3-2 的順序推導對提案實作順序），被處理的一端理由要寫出保留的位置；E4：與本次需求和重複都無關的元素保留原文；E5：`git status` 只有新增的紀錄目錄；E6：如有推定或適用範圍無法判斷的驗收條件，根因報告與藍圖展示中都有逐條詢問 |
| S2 | E2、E4、E5、E6 | 本次 | 以 `skill-engineering` 優化 `plan-with-class-diagram`：使用者回報 Phase 3 在確認提案後，又依類別圖重新推導實作順序，與提案中已確認的實作順序重複，日後改順序規則時要改兩處 | Phase 2 回覆「確認」；Phase 4 回覆「先不要施工」 | E2：根因報告把 P3-2 的缺陷類型直接歸入表中的「衝突」，沒有另外延伸定義；E4、E5、E6 同 S1 |
| S3 | E7 | 回歸 | P5-4 與 `施工委派` Rule 5 的施工差異處理 | 無 | 逐步演練：模擬 W2 被 `skill-form-rule` 回報差異，依 To-Be 的 P5-4 與施工委派檢查清單，會停止施工、記錄差異、返回 Phase 3、經 Phase 4 確認後只施工「待施工」的工作項。這個情境需要人為製造施工差異，無法在不帶脈絡的子代理中自然重現，所以改為逐步演練 |
| S4 | E3 | 本次 | 施工後的 `skill-engineering` | 無 | 以新版 `skill-form-sop` 的完成檢查清單逐步檢查 45 個步驟，沒有步驟重述自己的 RuleFile、樣板組或腳本；5 份 RuleFile 與 2 組樣板之間沒有重複存放的判準 |

直接呼叫者：無，不需要呼叫者的回歸情境。

## 8. 驗收結果

- 施工後結構診斷：`skill-engineering`、`plan-with-class-diagram`、`skill-form-sop` 都是 0 個 error、0 個 warning；`SKILL.md` 與 W1-SKILL.md、三份 RuleFile 與 W2–W4 逐字一致。
- 連結同步結果：10 個 Skill 連結正常，沒有衝突或缺漏；`skill-creator` 並存（既有）。
- 施工差異：2 次（見檔頭），都由新版 `skill-form-sop` 攔下，依 `施工委派` Rule 5 停止並重新取得確認。
- 驗收方式：S1、S2 在包含本次修改、但 `plan-with-class-diagram` 還原為 HEAD 版本的暫存專案複本中，由不帶本次分析脈絡的子代理執行 Phase 1–4；S3 以本次實際發生的兩次施工差異為證據；S4 以新版 `SOP-格式規範` Rule 4 逐步檢查。
- S1 **失敗**（E1）：
  - 子代理的藍圖（複本 `specs/skill-engineering/04-plan-with-class-diagram-提案缺少風險與待決問題/blueprint.md`）只處理與需求相關的元素。
  - 「保留」表以整條 Rule 或整段步驟為單位，只寫追溯：`類別圖輸出` Rule 1（E6、E7）、Rule 2（E7、E8）、Rule 6（E8）、P1-2～P1-4（E8、E2）、Phase 3（E10）。D1、D2、D3、D5 與 P1-2、P1-4 的重述都被保留。
  - 約束分配表 C6 已把提案結構分配給樣板組，但 Rule 1 仍被保留；C7 的存放位置同時寫了「Rule 6 與樣板組的圖例」，違反 `約束分配` Rule 1，沒有被發現。
  - E4（其餘元素保留原文）、E5（只新增紀錄目錄）、E6（推定的 E3–E5 逐條詢問）通過。
- S2 通過（E2、E4、E5、E6）：根因 R1、R2 直接歸入「衝突」，沒有延伸定義；另外依新的 `重新設計` Rule 2 多刪除了 Rule 2 第 4 條（D3）。但「保留」表同樣以整條 Rule 保留了 Rule 1、6 與 P1-1～P1-5，D2、D5 與 P1-2、P1-4 的重述沒有處理（S2 的判斷方式不含 E1）。
- S3 通過（E7）：本次實際發生兩次施工差異，都依 P5-4 與 `施工委派` Rule 5 停止、記錄、返回 Phase 3，經使用者確認後只施工「待施工」的工作項。
- S4 通過（E3）：施工後 45 個步驟與 5 個完成條件都通過新版 `SOP-格式規範` Rule 4 的逐步檢查。
- 結論：S1 失敗來自 Skill，依 P5-13 返回 Phase 2，見 `rca-report.md` 第 9 節。

## 9. 第二輪修改（R3、R4，S1 驗收失敗後）

- 根因：`rca-report.md` 第 9 節的 R3（約束分配表沒有涵蓋全部約束）與 R4（工程藍圖範例示範只寫追溯的保留表，範例的 To-Be SOP 也含有步驟重述）。
- 驗收條件：沿用 E1–E8，不變。
- To-Be SOP：`SKILL.md` 不再修改，與第 2 節相同。
- 確認紀錄：2026-09-25 使用者於 Phase 4 回覆「確認」：同意 R3、R4 與 W5–W7，以及新規則從下一次執行開始適用
- 適用時點：新規則從下一次執行開始適用；本藍圖依修改前的設計檢查清單產生，約束分配表只列本輪相關的約束。

### 差異

| 元素 | 動作 | 內容 | 追溯 |
| ---- | ---- | ---- | ---- |
| `rules/約束分配-格式規範.md` Rule 1 | 修改 | 第 1 條之後加入「約束分配表必須涵蓋 To-Be 中所有步驟內文、RuleFile、樣板組與腳本所表達的約束，包含本次沒有修改的既有約束。」；正反範例各加 Example 2：完整列出本次與既有約束，以及只列本次約束、既有約束存兩處卻沒被發現 | R3、E1（C16） |
| `rules/重新設計-格式規範.md` Rule 2 第 2、3 條與正反範例 | 修改 | 第 2 條加上「並列出該元素表達的每一條約束編號」；第 3 條改為「元素表達的約束，其存放位置不是該元素時，表達該約束的內容視為無法追溯，必須刪除或修改」；範例的追溯欄帶出約束編號，反例改為只寫追溯、沒有約束編號 | R3、E1（C17） |
| `templates/engineering-blueprint.md` | 修改 | 「保留」表加上「約束」欄 | R3、R4、E1（C18） |
| `templates/engineering-blueprint.example.md` | 修改 | To-Be SOP 改為不含重述的版本（P1-2～P1-4、P1-7、P3-2、P3-3）；「保留」表加上「約束」欄；約束分配表補齊 C4–C15（既有約束）；修改表與驗收結果帶出約束編號 | R4、E1，範例與 C16–C18 一致 |

### 淨增減

| 項目 | 第一輪後 | 第二輪後 | 增減 |
| ---- | -------- | -------- | ---- |
| Phase | 5 | 5 | 0 |
| 步驟 | 45 | 45 | 0 |
| RuleFile | 5 | 5 | 0 |
| Rule | 24 | 24 | 0 |
| 樣板組 | 2 | 2 | 0 |
| 腳本 | 3 | 3 | 0 |

- 條列 +1（`約束分配` Rule 1）：R3 是「不可檢查」，但「涵蓋全部約束」是原本沒有的要求，改寫既有條列無法表達。依 `重新設計` Rule 4 的順序考慮：沒有可刪除的內容；第 1 條「只指定一個存放位置」與「涵蓋範圍」是兩件事，改寫會讓第 1 條同時承擔兩個要求；樣板組只能固定表格欄位，無法保證列出的範圍完整。
- 樣板骨架 +1 欄：「列出約束編號」屬於產出檔結構的一部分，依約束分配 Rule 2 由樣板組固定欄位，`重新設計` Rule 2 規範欄位內容。

### 約束分配表（本輪）

| 編號 | 約束 | 所屬步驟 | 存放位置 | 理由 |
| ---- | ---- | -------- | -------- | ---- |
| C16 | 約束分配表涵蓋 To-Be 的全部約束，包含沒有修改的既有約束 | P3-3 | `rules/約束分配-格式規範.md` Rule 1 | 需要判斷元素表達了哪些約束，腳本無法判斷；樣板只能固定欄位；屬於約束分配主題 |
| C17 | 保留或修改的元素列出其表達的每一條約束編號；約束的存放位置不是該元素時，該內容無法追溯 | P3-4、P3-7 | `rules/重新設計-格式規範.md` Rule 2 | 需要判斷元素表達的約束，並需要正反例；屬於既有元素保留判斷的主題 |
| C18 | 「保留」表有「約束」欄 | P3-9 | 工程藍圖樣板組 | 產出檔的整體結構 |

### 施工工作項

| 順序 | 工作項 | 執行者 | 目標 | 內容 | 約束 | 狀態 |
| ---- | ------ | ------ | ---- | ---- | ---- | ---- |
| 5 | W5 | `skill-form-rule` | `rules/約束分配-格式規範.md` Rule 1 | 加入涵蓋範圍條列與 Example 2 | C16 | 已完成 |
| 6 | W6 | `skill-form-rule` | `rules/重新設計-格式規範.md` Rule 2 | 改寫第 2、3 條與範例 | C17 | 已完成 |
| 7 | W7 | `skill-form-template` | 工程藍圖樣板組 | 骨架加「約束」欄；範例依上表修改 | C18 | 已完成 |

- W5–W7 完成後的全文，分別與同目錄的 `W5-約束分配-格式規範.md`、`W6-重新設計-格式規範.md`、`W7-engineering-blueprint.md`、`W7-engineering-blueprint.example.md` 逐字一致。
- W5、W6 依 07 的新版 `skill-form-rule` 施工（07 先施工）。

### 驗收情境（第二輪）

| 編號 | 驗證 | 類型 | 輸入 | 預先回覆 | 判斷方式 |
| ---- | ---- | ---- | ---- | -------- | -------- |
| S1' | E1、E4、E5、E6 | 本次（重跑 S1） | 同 S1：新的暫存複本，`plan-with-class-diagram` 還原為 HEAD 版本，需求「提案加上風險與待決問題」 | 同 S1 | 同 S1 的判斷方式；另外檢查「保留」表每列都有「約束」欄，且約束分配表涵蓋 `plan-with-class-diagram` 的既有約束 |
| S2' | E2、E4、E5、E6 | 回歸（重跑 S2） | 同 S2 | 同 S2 | 同 S2 的判斷方式 |

### 第二輪驗收結果

- 施工後結構診斷：`skill-engineering`、`plan-with-class-diagram`、`skill-form-sop`、`skill-form-rule` 都是 0 個 error、0 個 warning；W5、W6 的 RuleFile 與 W7 的樣板組都與 To-Be 逐字一致；範例沒有殘留填位符號，標題結構與骨架一致。連結同步正常。
- 施工方式：W5、W6 由新版 `skill-form-rule`（07）施工，讀取了 `skill-engineering` 的其他部位並依 Rule 3 檢查，沒有差異；W7 由 `skill-form-template` 施工。
- 驗收方式：S1'、S2' 在全新的暫存專案複本（`scratchpad/accept2/`，`plan-with-class-diagram` 還原為 HEAD 版本）中，由不帶本次分析脈絡的子代理執行 Phase 1–4。
- S1' **部分通過**（E1 未完全滿足；E4、E5、E6 通過）：
  - 保留表每列都有「約束」欄；約束分配表涵蓋 25 條約束，包含全部既有約束。
  - 已處理：P1-2～P1-4 重述腳本（C16）、D1（P3-2）、D3（Rule 2 第 4 條合併到 Rule 5）。第一次 S1 這些都被保留。
  - 未處理：D2——C10 把 Rule 1 的「mermaid code block、`classDiagram` 開頭、文字在圖外、不用 `classDef`、語法檢查」打包成一條約束，整條分配給 Rule 1，重述骨架的部分被一起保留；D5——C14 同樣以整條 Rule 6 為一條約束。D4 經子代理判斷為角度不同而保留。
- S2' 通過（E2、E4、E5、E6）：根因直接歸入「衝突」；另外依新規則刪除了 D2（C9 樣板結構與 C10 `classDef`／語法檢查分開成兩條約束）、D3、D4 與 P1-2～P1-4 的重述；D5 經子代理判斷為「以名稱引用圖例」而保留。
- 比較：S1' 與 S2' 使用同一套規則，差別在約束的切分粒度；S2' 把存放位置不同的要求拆成不同約束，因此發現 D2，S1' 打包後沒有發現。見 `rca-report.md` 第 10 節。

## 10. 第三輪修改（R5，S1' 部分通過後）

- 根因：`rca-report.md` 第 10 節的 R5（約束的切分粒度沒有規定）。
- 確認紀錄：2026-09-25 使用者回覆「確認，開始執行」，同意加入「存放位置判斷不同的要求，必須拆成不同約束。」並接受 D5 不列入 E1 的判斷。
- 驗收條件：沿用 E1–E8；E1 的判斷項目移除 D5。

| 元素 | 動作 | 內容 | 追溯 |
| ---- | ---- | ---- | ---- |
| `rules/約束分配-格式規範.md` Rule 1 | 修改 | 涵蓋範圍條列之後加入「存放位置判斷不同的要求，必須拆成不同約束。」；Good／Bad Example 各加 Example 3：錯誤訊息 Rule 的欄位順序（樣板組）與訊息內容（RuleFile）拆開與打包的對照。範例只示範新條列，不新增要求 | R5、E1（C19） |

- 淨增減：條列 +1。R5 是「不可檢查」，但粒度是原本沒有的要求，第 1 條「只指定一個存放位置」與第 2 條「涵蓋範圍」都無法改寫出這個意思。
- 約束：C19「存放位置判斷不同的要求拆成不同約束」，所屬步驟 P3-3，存放於 `rules/約束分配-格式規範.md` Rule 1；需要判斷各要求的最佳存放位置，屬於約束分配主題。

| 順序 | 工作項 | 執行者 | 目標 | 內容 | 約束 | 狀態 |
| ---- | ------ | ------ | ---- | ---- | ---- | ---- |
| 8 | W8 | `skill-form-rule` | `rules/約束分配-格式規範.md` Rule 1 | 加入粒度條列與 Example 3；完成後與同目錄 `W8-約束分配-格式規範.md` 逐字一致 | C19 | 已完成 |

| 編號 | 驗證 | 類型 | 輸入 | 預先回覆 | 判斷方式 |
| ---- | ---- | ---- | ---- | -------- | -------- |
| S1'' | E1、E4、E5、E6 | 本次（重跑 S1） | 同 S1：全新的暫存複本，`plan-with-class-diagram` 還原為 HEAD 版本 | 同 S1 | 同 S1'，D5 不列入；另外檢查約束分配表中，存放位置判斷不同的要求是否拆開 |

### 第三輪驗收結果

- 施工後結構診斷：`skill-engineering` 0 個 error、0 個 warning；`rules/約束分配-格式規範.md` 與 `W8-約束分配-格式規範.md` 逐字一致。W8 由新版 `skill-form-rule` 施工，依 Rule 3 檢查後沒有差異。
- 驗收方式：S1'' 在全新的暫存專案複本（`scratchpad/accept3/05-S1rr`，`plan-with-class-diagram` 還原為 HEAD 版本）中，由不帶本次分析脈絡的子代理執行 Phase 1–4。
- S1'' **通過**（E1、E4、E5、E6）：
  - E1：刪除表涵蓋 P1-2～P1-4 的腳本重述、D1（P3-2）、D2（Rule 1 第 1–3 條與 Example 1，對應樣板組的 C8）、D3（Rule 2 第 4 條，保留 Rule 5）、D4（Rule 5 第 3 條，保留 `類別設計` Rule 4）。約束分配表把存放位置不同的要求拆開：樣板結構 C8、`classDef` C10、語法檢查 C11、Rule 6 的四項 C17–C20。D5 依使用者確認不列入判斷。
  - E4：與本次需求和重複都無關的元素保留原文，保留表每列都有「約束」欄。
  - E5：`git status` 只有新增的紀錄目錄。
  - E6：推定的 E3–E7 在 Phase 2 閘門逐條詢問。
- 觀察（既有，與本次修改無關，留待使用者決定是否另外優化）：
  1. S2' 與 S1'' 的子代理都在 Phase 3 發現既有核心預期漏列，導致既有 Rule 無法追溯；SOP 在 Phase 3 沒有返回 Phase 1 的分支，兩者都自行返回 Phase 1 補上。
  2. `audit_skill.py` 把 `.claude/skills` 下指向 `.agents/skills` 的連結列為 `shadowed_skills`，容易被誤讀為衝突。
  3. Phase 4 回覆未確認時，SOP 沒有規定是否把回覆寫入藍圖。
- 結論：05 的驗收情境 S1''、S2'、S3、S4 全部通過，E1–E7 均已滿足；E8（個案）：SOP 仍為 45 個步驟，步驟內文總長度由 4390 字降為 4241 字。
