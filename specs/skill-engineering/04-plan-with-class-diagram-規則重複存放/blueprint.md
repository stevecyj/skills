# 工程藍圖：plan-with-class-diagram－規則重複存放

- 模式：優化
- 紀錄目錄：`specs/skill-engineering/04-plan-with-class-diagram-規則重複存放/`
- 根因報告：同一紀錄目錄的 `rca-report.md`；使用者已確認判斷標準、R1–R8（「好，執行」）
- 確認紀錄：2026-09-25 使用者於 Phase 4 回覆「開始施工」：同意全部刪除項目、施工工作項、施工順序（06 → 05 → 04），以及新增的既有驗收條件（04 E9；05 E6、E7；06 E2–E4）與個案驗收條件 05 E8

## 1. 驗收條件

| 編號 | 驗收條件 | 來源 | 適用範圍 |
| ---- | -------- | ---- | -------- |
| E1 | `SKILL.md` 每個步驟只包含控制平面：執行條件、讀取或委派的資源、要完成的決策或產物、停止或分支位置，以及只屬於該步驟且沒有其他存放位置的約束；步驟讀取或委派的資源已規範的內容，不在步驟中重述 | 本次 | 通用 |
| E2 | 每個具體判準只存在於一個資源檔：RuleFile 不重述樣板組固定的結構與圖例，同一判準不在兩條 Rule 中各寫一次 | 本次 | 通用 |
| E3 | 提案檔依序包含「類別圖」與「設計說明與實作順序」兩個頂層 Section，並寫入 `specs/plan/<NN>-<計畫名稱>/class-diagram-proposal.md` | 既有 | 通用 |
| E4 | 類別圖通過 Mermaid 語法檢查且不含 `classDef` 或 `style`；類別圖、設計說明與實作順序使用相同的類別名稱，描述的變更類型也一致 | 既有 | 通用 |
| E5 | 每個類別帶有一個圖例中的類別標記；`<<modified>>` 類別至少有一個帶標記的成員；成員與關係標記的位置符合圖例 | 既有 | 通用 |
| E6 | 實作順序把被依賴者排在依賴者之前，刪除步驟排在所有依賴者改寫之後 | 既有 | 通用 |
| E7 | 使用者明確確認提案前，不修改任何程式碼 | 既有 | 通用 |
| E8 | 使用者確認後，程式碼依已確認提案的實作順序實作並回報驗證結果；實際狀態需要改變類別圖時，返回 Phase 1 重新取得確認 | 既有 | 通用 |
| E9 | 需求涉及 Java 原始碼時，以類別結構腳本取得有上限的摘要，並檢視未解析引用與截斷狀態後再閱讀相關程式碼 | 既有 | 通用 |

## 2. To-Be SOP

````markdown
---
name: plan-with-class-diagram
description: Before implementing a feature or refactor that introduces or changes classes, propose a Mermaid class diagram, obtain explicit user confirmation, then implement according to the approved diagram and dependency order.
---

# SOP

## Phase 1 -- 分析與提案

1. `READ` 使用者需求、專案規範、Java 原始碼路徑（若有），以及專案根目錄下既有的 `specs/plan/` 計畫目錄（若存在），建立「專案輸入」。
2. `THINK` 若「專案輸入」顯示本次需求涉及 Java 原始碼，依需求與原始碼路徑選定完整名稱的入口類別。
3. `DELEGATE` 若已選定 Java 入口類別，在本 Skill 目錄執行 `uv run --script scripts/inspect_java_classes.py --project-root <專案根目錄絕對路徑> --entry <完整類別名稱>`，從標準輸出取得 JSON 類別結構摘要；若缺少 `uv`，依 https://docs.astral.sh/uv/getting-started/installation/ 的目前平台安裝說明安裝後重試，安裝失敗時回報原因並停止此步驟。
4. `CHECK` 執行腳本時確認退出狀態為零且標準輸出為可解析的 JSON，並檢視未解析引用及截斷狀態；失敗時依標準錯誤輸出修正入口並重試，仍失敗時回報原因。
5. `READ` 已取得類別結構摘要時依摘要選擇相關原始碼深入閱讀；未取得摘要時直接閱讀相關程式碼，建立「相關程式碼現況」。
6. `READ` 設計類別時，讀取 `rules/類別設計-格式規範.md` 的所有 Rule，建立類別設計檢查清單。
7. `THINK` 依「相關程式碼現況」與已載入的類別設計檢查清單，設計類別與實作順序；並決定本次計畫名稱及下一個可用的兩位數編號 `<NN>`（從既有最大編號加一，沒有既有計畫時從 `01` 開始）。
8. `CHECK` 逐條驗證類別設計檢查清單；未通過時修正設計。
9. `READ` 產生類別圖時，讀取 `rules/類別圖輸出-格式規範.md` 的所有 Rule，建立類別圖輸出檢查清單。
10. `READ` `templates/class-diagram-proposal.md` 與 `templates/class-diagram-proposal.example.md`，載入骨架與範例，命名載入結果為「類別圖提案樣板組」。
11. `WRITE` 依已載入的類別圖輸出檢查清單與「類別圖提案樣板組」，建立計畫目錄，將完整 Markdown 類別圖提案檔寫入專案根目錄下的 `specs/plan/<NN>-<本次計畫名稱>/class-diagram-proposal.md`。
12. `CHECK` 逐條驗證類別圖輸出檢查清單，並確認提案檔位於指定路徑且涵蓋需求。

完成條件：類別設計與類別圖輸出檢查清單均已通過，完整 Markdown 類別圖提案檔可供使用者評估，且程式碼尚未修改。

## Phase 2 -- 確認提案

1. `WRITE` 向使用者展示完整 Markdown 類別圖提案檔，並請求明確確認。
2. `CHECK` 使用者回覆；要求修改時返回 Phase 1，未明確確認時停止執行。

完成條件：使用者已明確確認目前版本的 Markdown 類別圖提案檔與實作順序。

## Phase 3 -- 按圖開發與驗證

1. `READ` 已確認的 Markdown 類別圖提案檔、最新的相關程式碼與專案規範。
2. `THINK` 比對最新的相關程式碼與已確認的 Markdown 類別圖提案檔；實際狀態需要改變類別圖時，返回 Phase 1 修訂並重新取得確認。
3. `WRITE` 依已確認的 Markdown 類別圖提案檔中的實作順序修改程式碼。
4. `CHECK` 類別組織符合已確認的 Markdown 類別圖提案檔中的類別圖，並執行適用的測試、型別檢查與 lint。

完成條件：程式碼符合已確認的 Markdown 類別圖提案檔中的類別圖，且所有適用驗證結果已回報。
````

## 3. 差異表

### 刪除

| 元素 | 位置 | 理由 |
| ---- | ---- | ---- |
| 「類別圖必須放在語言標記為 `mermaid` 的 code block」「除了樣板固定的 `%%{init}%%` 指令外，Mermaid code block 的第一個非空白內容必須是 `classDiagram`」「設計說明與實作順序必須放在 Mermaid code block 之外」三條列，以及只示範這三條列的 Good／Bad Example 1 | `rules/類別圖輸出-格式規範.md` Rule 1 | R5：約束已由類別圖提案樣板組的骨架固定（C1），只保留樣板組 |
| 「新類別的名稱必須在類別圖、設計說明與實作順序中保持一致」 | 同檔 Rule 2 | R6：與 Rule 5「設計說明與實作順序必須使用類別圖中的類別名稱」為同一約束（C3），只保留 Rule 5 |
| 「實作順序必須與類別圖所呈現的相依性一致」 | 同檔 Rule 5 | R7：順序與相依性由 `類別設計` Rule 4 規範（C4）；圖文矛盾由 Rule 5「不得描述互相矛盾的責任或關係」涵蓋 |

### 修改、合併、改換存放位置與新增

| 元素 | 動作 | 內容 | 追溯 |
| ---- | ---- | ---- | ---- |
| `rules/類別圖輸出-格式規範.md` Rule 1 標題與範例 | 修改 | 標題改為「類別圖只能使用 Mermaid 10.2.3 可解析的語法」，保留原第 4、5 條；原 Example 2 成為唯一的 Good／Bad Example | E4（C2） |
| 同檔 Rule 5 正反範例 | 修改 | 範例說明刪除「實作順序對應相依性」的描述；Bad Example 的實作順序改為 `OrderStore`、`OrderService`，只示範名稱與變更類型不一致 | 範例不得與刪除後的條列矛盾 |
| 同檔 Rule 6 第 1、4、5 條 | 修改 | 不再列舉標記名稱，改為引用「圖例中其中一種類別標記」「圖例中可標在成員的標記」「圖例中可標在關係的標記」 | R8、E5（C5、C6） |
| P1-2 | 修改 | 刪除「及最多輸出類別數（預設 30）」 | R1、E1、E9（C11） |
| P1-3 | 修改 | 命令刪除 `--max-classes <最多類別數>`，上限由腳本預設值決定 | R1、E9（C11） |
| P1-4 | 修改 | 「確認退出狀態為零、JSON 包含入口類別、輸出類別數未超過上限」改為「確認退出狀態為零且標準輸出為可解析的 JSON」；「檢視未解析引用及截斷狀態」保留 | R2、E1、E9（C11） |
| P1-7 | 修改 | 「設計必要的類別、責任、關係、相依性與開發順序」改為「設計類別與實作順序」；計畫編號的文字保留原文 | R3、E1、E6（C4、C9） |
| P3-2 | 修改 | 改為比對最新的相關程式碼與已確認提案，保留返回 Phase 1 的分支，不再重新確定實作順序 | R4、E8（C8） |
| P3-3 | 修改 | 改為依已確認提案中的實作順序修改程式碼 | R4、E8（C7） |

### 保留

| 元素 | 追溯 |
| ---- | ---- |
| frontmatter | E7、E8：description 已說明先確認提案再依相依順序實作；本次不影響觸發情境 |
| 三個 Phase 的完成條件 | Phase 1：E3–E7；Phase 2：E7；Phase 3：E8；只概述結果，不重述判斷細節 |
| P1-1、P1-5、P1-6、P1-8～P1-11 | E3–E6、E9；步驟內文只含控制平面 |
| P1-12（含「涵蓋需求」） | E3；「涵蓋需求」沒有其他存放位置（H9） |
| P2-1、P2-2、P3-1、P3-4 | E7、E8 |
| `rules/類別圖輸出-格式規範.md` Rule 2 其餘條列、Rule 3、Rule 4、Rule 5 其餘條列、Rule 6 第 2、3、6 條 | E4、E5；各自是其約束的唯一存放位置 |
| `rules/類別設計-格式規範.md` Rule 1～4 | E6；Rule 3（關係語意）與 `類別圖輸出` Rule 4（符號對應）約束的對象不同（H11） |
| 類別圖提案樣板組 | E3、E5；C1、C5 的唯一存放位置 |
| `scripts/inspect_java_classes.py` | E9；C11 的唯一存放位置 |

## 4. 淨增減

| 項目 | As-Is | To-Be | 增減 |
| ---- | ----- | ----- | ---- |
| Phase | 3 | 3 | 0 |
| 步驟 | 18 | 18 | 0 |
| RuleFile | 2 | 2 | 0 |
| Rule | 10 | 10 | 0 |
| 樣板組 | 1 | 1 | 0 |
| 腳本 | 1 | 1 | 0 |

- 內容減少：`類別圖輸出` 刪除 5 條條列（Rule 1 三條、Rule 2 一條、Rule 5 一條）與 Rule 1 的一組正反範例；Rule 6 三條條列改為引用圖例；SOP 6 個步驟縮短（步驟內文 1470 字降為 1406 字）。沒有任何新增。

## 5. 約束分配表

| 編號 | 約束 | 所屬步驟 | 存放位置 | 理由 |
| ---- | ---- | -------- | -------- | ---- |
| C1 | 類別圖位於 `mermaid` code block、以 `classDiagram` 開始，設計說明與實作順序位於圖外 | P1-11 | 類別圖提案樣板組 | 約束的是提案檔的整體結構，骨架已固定；腳本不需要；因此不放 RuleFile |
| C2 | 不得使用 `classDef`、`style`，展示前通過 Mermaid 語法檢查 | P1-11、P1-12 | `類別圖輸出` Rule 1 | 需要判斷圖中陳述式；語法檢查需要 Mermaid 執行環境，建立腳本的成本高於要處理的問題（03 紀錄 C11）；樣板無法限制填入的內容 |
| C3 | 類別圖、設計說明與實作順序使用相同名稱 | P1-11 | `類別圖輸出` Rule 5 | 圖文對應主題；需要判斷語意，需要正反例 |
| C4 | 實作順序依相依性排列，刪除排在依賴者改寫之後 | P1-7 | `類別設計` Rule 4 | 需要判斷相依關係與正反例；屬於類別設計主題 |
| C5 | 標記詞彙、意義與可標位置 | P1-11 | 類別圖提案樣板組（圖例） | 每份提案相同的固定內容 |
| C6 | 每個類別的標記判定、標記位置、annotation 合併、`<<modified>>` 至少一個帶標記成員、關係標記的適用範圍 | P1-11 | `類別圖輸出` Rule 6 | 需要依設計語意逐類別判斷；樣板與腳本都無法保證 |
| C7 | 確認後依已確認提案的實作順序修改程式碼 | P3-3 | SOP 流程 | 屬於執行順序 |
| C8 | 實際狀態需要改變類別圖時返回 Phase 1 重新確認 | P3-2 | SOP 流程 | 屬於分支與 Phase 轉移 |
| C9 | 計畫編號：既有最大編號加一，沒有時從 `01` 開始 | P1-7 | 步驟內文 | 只屬於單一步驟，一句話即可表達（H9） |
| C10 | 提案檔涵蓋需求 | P1-12 | 步驟內文 | 只屬於單一步驟，一句話即可表達（H9） |
| C11 | 摘要上限的預設值、找不到入口時失敗、輸出數量不超過上限 | P1-2～P1-4 | `scripts/inspect_java_classes.py` | 輸入輸出明確且可由程式判斷；腳本已經實作 |

## 6. 施工工作項

| 順序 | 工作項 | 執行者 | 目標 | 內容 | 約束 | 狀態 |
| ---- | ------ | ------ | ---- | ---- | ---- | ---- |
| 1 | W1 | `skill-form-sop` | Phase 1 -- 分析與提案中選定入口類別的 `THINK`、執行類別結構腳本的 `DELEGATE`、檢查腳本結果的 `CHECK`、設計類別的 `THINK`；Phase 3 -- 按圖開發與驗證中確定實作順序的 `THINK` 與修改程式碼的 `WRITE` | 依 To-Be SOP 改寫 6 個步驟 | C4、C7–C9、C11 | 已完成 |
| 2 | W2 | `skill-form-rule` | `rules/類別圖輸出-格式規範.md` | 修改 Rule 1、2、5、6 的條列與範例 | C1–C3、C5、C6 | 已完成 |

### 工作項內容

#### W1：SOP 修改

依第 2 節 To-Be SOP 修改下列 6 個步驟，其餘步驟保留原文：

| 步驟 | 修改後全文 |
| ---- | ---------- |
| Phase 1 -- 分析與提案中選定入口類別的 `THINK` 步驟 | `THINK` 若「專案輸入」顯示本次需求涉及 Java 原始碼，依需求與原始碼路徑選定完整名稱的入口類別。 |
| Phase 1 -- 分析與提案中執行 `inspect_java_classes.py` 的 `DELEGATE` 步驟 | 命令刪除 ` --max-classes <最多類別數>`，其餘文字不變 |
| Phase 1 -- 分析與提案中檢查腳本結果的 `CHECK` 步驟 | `CHECK` 執行腳本時確認退出狀態為零且標準輸出為可解析的 JSON，並檢視未解析引用及截斷狀態；失敗時依標準錯誤輸出修正入口並重試，仍失敗時回報原因。 |
| Phase 1 -- 分析與提案中「設計必要的類別、責任、關係…」的 `THINK` 步驟 | `THINK` 依「相關程式碼現況」與已載入的類別設計檢查清單，設計類別與實作順序；並決定本次計畫名稱及下一個可用的兩位數編號 `<NN>`（從既有最大編號加一，沒有既有計畫時從 `01` 開始）。 |
| Phase 3 -- 按圖開發與驗證中「確定實作順序」的 `THINK` 步驟 | `THINK` 比對最新的相關程式碼與已確認的 Markdown 類別圖提案檔；實際狀態需要改變類別圖時，返回 Phase 1 修訂並重新取得確認。 |
| Phase 3 -- 按圖開發與驗證中修改程式碼的 `WRITE` 步驟 | `WRITE` 依已確認的 Markdown 類別圖提案檔中的實作順序修改程式碼。 |

#### W2：`rules/類別圖輸出-格式規範.md`

1. Rule 1：標題改為「類別圖只能使用 Mermaid 10.2.3 可解析的語法」；刪除第 1–3 條（`mermaid` code block、`classDiagram` 開頭、說明位於圖外），保留 `classDef`／`style` 與語法檢查兩條；刪除 Good／Bad Example 1，原 Example 2 改為唯一範例，不再使用 `### Example N` 小標。
2. Rule 2：刪除「新類別的名稱必須在類別圖、設計說明與實作順序中保持一致」。範例不變。
3. Rule 5：刪除「實作順序必須與類別圖所呈現的相依性一致」。Good Example 說明改為「設計說明與實作順序使用圖中的名稱，並註明與標記一致的變更類型。」Bad Example 說明改為「配套文字改用圖中不存在的名稱 `OrderStore`，並把圖中標示 `<<modified>>` 的 `OrderService` 寫成新增。」Bad Example 的實作順序改為「`OrderStore`、`OrderService`」。
4. Rule 6：第 1 條改為「每個類別必須在類別本體第一行，以圖例中其中一種類別標記標示，並以 `:::` 套用圖例中對應的樣式類別。」第 4 條的「`«new»`、`«changed»` 或 `«removed»`」改為「圖例中可標在成員的標記」；第 5 條的「`«new»` 或 `«removed»`」改為「圖例中可標在關係的標記」。範例不變。

完成後全文應與 `rca-report.md` 同目錄下的 `W2-類別圖輸出-格式規範.md` 逐字一致。

## 7. 驗收情境

| 編號 | 驗證 | 類型 | 輸入 | 預先回覆 | 判斷方式 |
| ---- | ---- | ---- | ---- | -------- | -------- |
| S1 | E1–E7、E9 | 本次 | 包含本次修改的 HEAD 專案複本，需求：為 `java-web-framework` 加入全域例外對應，讓 Controller 拋出的特定例外可以由註冊的對應器轉成指定的 HTTP 狀態碼與訊息 | Phase 2 回覆「先不要實作」 | E1、E2：施工後的 Skill 以新版 `skill-form-sop` 完成檢查清單逐步檢查，R1–R8 的重複都只剩一處，且沒有新的重述；E3：提案檔位於 `specs/plan/04-<名稱>/class-diagram-proposal.md`，兩個頂層 Section 依序出現；E4：以 `mermaid@10.2.3` 解析通過，沒有 `classDef`／`style`，名稱與變更類型在圖文中一致；E5：每個類別恰有一個圖例中的類別標記，`<<modified>>` 類別至少有一個帶成員標記的成員；E6：被依賴者先於依賴者；E7：`git status` 只有新增的提案目錄；E9：子代理回報執行腳本時沒有傳入 `--max-classes`，並說明截斷與未解析引用的檢視結果 |
| S2 | E8 | 回歸 | 包含本次修改的 HEAD 專案複本，需求：讓 `Response` 可以設定自訂回應標頭，並在 `JdkHttpServerAdapter` 送出 | Phase 2 回覆「確認」 | 程式碼修改的順序與已確認提案的實作順序一致，子代理回報 Phase 3 沒有另外推導順序；實作的類別組織符合類別圖；回報 `mvn test` 結果 |

直接呼叫者：無，不需要呼叫者的回歸情境。

## 8. 驗收結果

- 施工後結構診斷：`plan-with-class-diagram` 0 個 error、0 個 warning；`SKILL.md` 與第 2 節 To-Be SOP 逐字一致；`rules/類別圖輸出-格式規範.md` 與 `W2-類別圖輸出-格式規範.md` 逐字一致。本次沒有改動任何 Mermaid 範例本體（Rule 1 保留的是原 Example 2），不需要重新渲染。
- 連結同步結果：Claude Code 已安裝；10 個 Skill 連結正常，沒有新建、移除、衝突、缺漏或移轉建議；`skill-creator` 在 `.agents/skills` 與 `.claude/skills` 中並存（既有狀況，與本次無關）。
- 施工方式：W1 由新版 `skill-form-sop`（06 已施工）施工，讀取了目標 Skill 的兩份 RuleFile、樣板組與腳本介面，並以新的 `SOP-格式規範` Rule 4 逐步檢查 To-Be，沒有回報差異。
- 驗收方式：兩個情境各自在包含本次未提交修改的暫存專案複本中執行（排除 04–06 紀錄目錄，執行前先提交基準，`git status` 為乾淨），由不帶本次分析脈絡的子代理實際執行 Skill。
- S1 通過（E1–E7、E9）：
  - 需求「全域例外對應」；提案檔寫入 `specs/plan/04-exception-mapping/class-diagram-proposal.md`，兩個頂層 Section 依序出現（E3）。
  - 以 `mermaid@10.2.3` 解析通過；子代理用含 `classDef` 的反例確認檢查器會失敗。圖文的變更類型一致（E4）。
  - 8 個類別各有一個標記；`Router`、`WebApplication` 為 `<<modified>>` 並帶 `«changed»`／`«new»` 成員（E5）。
  - 實作順序為 `ExceptionMapper` → `ExceptionMappers` → `Router` → `WebApplication` → 測試（E6）。
  - `git status` 只有新增的提案目錄（E7）。
  - 腳本命令沒有 `--max-classes`，上限由預設值 30 決定；子代理檢視了 `truncated: false` 與 36 筆未解析引用（都是 JDK 型別）（E9）。
  - E1、E2 由施工後的逐步檢查確認：R1–R8 的重複都只剩一處，新版 `skill-form-sop` 沒有找到新的重述。
- S2 通過（E8）：
  - 需求「Response 自訂回應標頭」；確認後，子代理引用 P3-3「依已確認的 Markdown 類別圖提案檔中的實作順序修改程式碼」，依提案的 3 步順序修改，沒有另外推導順序。
  - 程式與類別圖一致（`JdkHttpServerAdapter`、`Router`、`Handler` 沒有改動）；`mvn test` 15 個測試全部通過；把 `Response` 換回舊版時新測試會失敗，證明測試有效。
  - 子代理發現需求的功能其實已經存在（`Response.withHeader`），因此依 `類別設計` Rule 1 改為處理實測找到的三個限制，這個判斷經過確認關卡。這是需求與現況的落差，與本次修改無關。
- 觀察（既有問題，與本次修改無關，留待使用者決定是否另外優化）：
  1. P1-3 的 `--project-root` 應傳 Java 模組目錄還是 Skill 的專案根目錄，兩個子代理的做法不同。
  2. 腳本只追蹤繼承、欄位與方法簽章，不追蹤方法本體內的呼叫；`truncated: false` 容易被誤讀為摘要完整。
  3. 設計說明中的示範程式碼若出現圖外的類別名稱，`類別圖輸出` Rule 5 沒有說明是否適用。
  4. 需求已經滿足時，SOP 沒有說明處理方式。
- 真實專案副作用：`git status` 只包含本次施工的 Skill 檔案與 04–06 紀錄目錄。
