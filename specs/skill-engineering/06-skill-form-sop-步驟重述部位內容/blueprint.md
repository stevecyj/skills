# 工程藍圖：skill-form-sop－步驟重述部位內容

- 模式：優化
- 紀錄目錄：`specs/skill-engineering/06-skill-form-sop-步驟重述部位內容/`
- 根因報告：同一紀錄目錄的 `rca-report.md`；使用者已確認攔截點與 R1、R2（「好，執行」）
- 施工後發現：2026-09-25 施工 07 時，依同一標準回頭檢查，`skill-form-sop` P1-3「將工作拆成必要的 Phase、步驟與完成條件」的「必要的」重述 `SOP-格式規範` Rule 4「SOP 只保留必要步驟」，06 施工時沒有發現；列入第二輪修改
- 確認紀錄：2026-09-25 使用者於 Phase 4 回覆「開始施工」：同意全部刪除項目、施工工作項、施工順序（06 → 05 → 04），以及新增的既有驗收條件（04 E9；05 E6、E7；06 E2–E4）與個案驗收條件 05 E8

## 1. 驗收條件

| 編號 | 驗收條件 | 來源 | 適用範圍 |
| ---- | -------- | ---- | -------- |
| E1 | `skill-form-sop` 寫入或修改 SOP 後，每個步驟都不重述該步驟讀取或委派之部位已規範的內容；呼叫者提供的已確認設計含有重述時，向呼叫者回報差異並停止 | 本次 | 通用 |
| E2 | 只屬於單一步驟、沒有存放在任何部位的約束（例如胖 SOP 中待抽取的約束、計畫編號規則）仍可寫在步驟內文 | 既有 | 通用 |
| E3 | SOP 其餘格式要求不受影響：固定標題結構、受控動詞、完成條件、按需載入 | 既有 | 通用 |
| E4 | 被 `skill-derive-rule`、`skill-derive-template`、`skill-derive-script`、`skill-engineering` 委派時，依呼叫者已確認的設計寫入 SOP，不另行設計 | 既有 | 通用 |

## 2. To-Be SOP

````markdown
---
name: skill-form-sop
description: 撰寫或修改 Skill 的 SKILL.md 中 SOP 流程（Phase、步驟、完成條件與部位載入步驟）時，必須使用此 Skill；使用者提到 SOP、流程、Phase、workflow steps 時使用。撰寫 frontmatter 時改用 skill-form-description；建立新 Skill 或整體優化既有 Skill 時改用 skill-engineering。
---

# SOP

## Phase 1 -- 理解與設計

1. `READ` 使用者需求，以及修改既有 Skill 時的目標 `SKILL.md`、其 SOP 載入的 RuleFile 與樣板組，以及其 SOP 委派之腳本的介面說明，確認 Skill 的目的、輸入、輸出與限制。
2. `READ` `rules/SOP-格式規範.md` 的所有 Rule；SOP 需要引用 `rules/`、`templates/` 或 `scripts/` 的部位時，另讀取 `rules/SOP-按需載入-格式規範.md` 的所有 Rule，建立完成檢查清單。
3. `THINK` 依已載入的完成檢查清單，將工作拆成必要的 Phase、步驟與完成條件；呼叫者提供已確認的設計（例如工程藍圖的工作項內容，或 skill-derive-* 的已確認提案）時，以該設計為內容，不另行設計，該設計不符合完成檢查清單時向呼叫者回報差異並停止。

完成條件：已定義符合完成檢查清單的 SOP 結構。

## Phase 2 -- 寫入與驗證

1. `WRITE` 依已載入的完成檢查清單，在目標 `SKILL.md` 建立或更新唯一的 `# SOP` section。
2. `CHECK` 逐條驗證完成檢查清單；若有任一項未通過，修正後重新檢查。

完成條件：所有檢查項目均已通過。
````

## 3. 差異表

### 刪除

| 元素 | 位置 | 理由 |
| ---- | ---- | ---- |
| 無 | — | 依 `重新設計` Rule 2 逐一檢查 As-Is，沒有無法追溯或重複存放的元素 |

### 修改、合併、改換存放位置與新增

| 元素 | 動作 | 內容 | 追溯 |
| ---- | ---- | ---- | ---- |
| P1-1 | 修改 | 讀取範圍加上「其 SOP 載入的 RuleFile 與樣板組，以及其 SOP 委派之腳本的介面說明」 | R2、E1（C2） |
| `rules/SOP-格式規範.md` Rule 4 標題 | 修改 | 「SOP 只保留必要步驟」改為「SOP 只保留必要步驟與控制內容」 | R1、E1 |
| `rules/SOP-格式規範.md` Rule 4 第 3 條 | 修改 | 「SOP 不得重複已由參考檔定義的規則內容」改寫為兩條：步驟內文只能描述控制平面，以及只屬於本步驟且沒有存放在任何部位的約束；步驟讀取或委派的部位已規範的內容不得重述 | R1、E1、E2（C1） |
| `rules/SOP-格式規範.md` Rule 4 正反範例 | 修改 | 原範例改為 Example 1；Good／Bad 各加入 Example 2：載入錯誤訊息規則後，寫入步驟只引用檢查清單，以及同一步驟重述判準的反例 | R1、E1 |

### 保留

| 元素 | 追溯 |
| ---- | ---- |
| frontmatter | E3、E4：description 的觸發與分流不受影響 |
| P1-2、P1-3、Phase 2 的步驟與兩個 Phase 的完成條件 | E3、E4；P1-3 已規定以呼叫者的設計為內容，不符合時回報差異並停止；P2-2 已逐條驗證完成檢查清單；步驟內文只含控制平面 |
| `rules/SOP-格式規範.md` Rule 1–3、Rule 5 與 Rule 4 第 1、2 條 | E3 |
| `rules/SOP-按需載入-格式規範.md` Rule 1、2 | E3；Rule 1 第 9 條禁止重複路徑，與新條列禁止重述內容，約束的對象不同（H2） |

## 4. 淨增減

| 項目 | As-Is | To-Be | 增減 |
| ---- | ----- | ----- | ---- |
| Phase | 2 | 2 | 0 |
| 步驟 | 5 | 5 | 0 |
| RuleFile | 2 | 2 | 0 |
| Rule | 7 | 7 | 0 |
| 樣板組 | 0 | 0 | 0 |
| 腳本 | 0 | 0 | 0 |

- 條列 +1：R1 的缺陷類型是「不可檢查」，依 `重新設計` Rule 3 應把原有要求改寫為可檢查的描述。原本那一條同時承擔「可以寫什麼」與「不能重述什麼」兩層意思，改寫成兩條時，兩者都有可觀察的判斷方式；沒有新增原本沒有的要求。
- 範例：Rule 4 正反範例各增加一組。
- P1-1 步驟內文增加讀取的輸入（控制平面），步驟內文由 440 字增為 483 字。

## 5. 約束分配表

| 編號 | 約束 | 所屬步驟 | 存放位置 | 理由 |
| ---- | ---- | -------- | -------- | ---- |
| C1 | 步驟內文只能描述執行條件、讀取或委派的部位、要完成的決策或產物、停止或分支位置，以及只屬於本步驟且沒有存放在任何部位的約束；已讀取或委派的部位規範的內容不得重述 | P2-1、P2-2 | `rules/SOP-格式規範.md` Rule 4 | 需要判斷步驟內文與部位內容是否表達同一件事，腳本無法判斷語意；不是產出檔的整體結構；需要正反例；屬於 SOP 必要內容的既有主題，改寫既有條列 |
| C2 | 修改既有 Skill 時，讀取 SOP 載入或委派的部位 | P1-1 | 步驟內文 | 屬於讀取哪些輸入，是控制平面 |

## 6. 施工工作項

| 順序 | 工作項 | 執行者 | 目標 | 內容 | 約束 | 狀態 |
| ---- | ------ | ------ | ---- | ---- | ---- | ---- |
| 1 | W1 | `skill-form-sop` | `skill-form-sop` 的 `SKILL.md` 中 Phase 1 -- 理解與設計讀取使用者需求的 `READ` 步驟 | 擴大讀取範圍 | C2 | 已完成 |
| 2 | W2 | `skill-form-rule` | `rules/SOP-格式規範.md` Rule 4 | 改寫標題、第 3 條與正反範例 | C1 | 已完成 |

### 工作項內容

#### W1：SOP 修改

| 步驟 | 修改後全文 |
| ---- | ---------- |
| Phase 1 -- 理解與設計中讀取使用者需求的 `READ` 步驟 | `READ` 使用者需求，以及修改既有 Skill 時的目標 `SKILL.md`、其 SOP 載入的 RuleFile 與樣板組，以及其 SOP 委派之腳本的介面說明，確認 Skill 的目的、輸入、輸出與限制。 |

#### W2：`rules/SOP-格式規範.md` Rule 4

- 標題改為「SOP 只保留必要步驟與控制內容」。
- 第 3 條改寫為兩條：
  - 「步驟內文只能描述執行條件、讀取或委派的部位、要完成的決策或產物、停止或分支的位置，以及只屬於本步驟且沒有存放在任何部位的約束。」
  - 「步驟讀取或委派的部位已規範的內容，不得在步驟內文重述。」
- Good Example：原範例加上 `### Example 1`；新增 `### Example 2`，說明「載入錯誤訊息規則後，寫入步驟只引用檢查清單，判準只存在於 RuleFile。」並示範 `READ` 載入 `rules/API-錯誤訊息-格式規範.md` 與只引用檢查清單的 `WRITE`。
- Bad Example：原範例加上 `### Example 1`；新增 `### Example 2`，說明「同一情境中，寫入步驟在載入規則後又重述 RuleFile 已規範的判準；日後修改判準時必須同步修改 RuleFile 與步驟。」並示範 `WRITE` 在引用檢查清單後又寫出「訊息必須指出失敗欄位、收到的值與預期格式」。

完成後全文應與同目錄下的 `W2-SOP-格式規範.md` 逐字一致。

## 7. 驗收情境

情境在包含本次未提交修改的暫存專案複本中執行，並在複本的 `.agents/skills/` 下建立測試用的 Skill。

| 編號 | 驗證 | 類型 | 輸入 | 預先回覆 | 判斷方式 |
| ---- | ---- | ---- | ---- | -------- | -------- |
| S1 | E1、E2、E3 | 本次 | 測試用 Skill `release-notes-writer`：`WRITE` 步驟載入 `rules/發行說明-格式規範.md`（規定每個項目以動詞開頭並附 PR 編號）後，又寫出「每個項目必須以動詞開頭，並附上 PR 編號」；另一個步驟含有只屬於本步驟的「檔名使用 `YYYY-MM-DD`」。需求：使用 `skill-form-sop` 在 Phase 1 最後新增一個 `WRITE` 步驟，把發行說明寫入 `CHANGELOG.md` | 無 | 新步驟已加入；重述 RuleFile 判準的那句話被刪除；「檔名使用 `YYYY-MM-DD`」保留；SOP 仍符合標題、受控動詞、完成條件與按需載入規範 |
| S2 | E1、E2、E4 | 回歸（呼叫者 `skill-derive-rule`） | 測試用 Skill `commit-message-writer`：`WRITE` 步驟內文寫著「subject 不得超過 50 字元，並使用祈使句」（沒有對應的 RuleFile）；另一個步驟含有只屬於本步驟的「在 `.git/COMMIT_EDITMSG` 寫入草稿」。需求：使用 `skill-derive-rule` 為這個 `WRITE` 步驟展開 RuleFile | Before／After 回覆「確認」 | 新 RuleFile 包含兩項判準；抽取後的 `WRITE` 步驟只引用檢查清單，沒有殘留「50 字元」「祈使句」；只屬於本步驟的約束保留；`skill-form-sop` 依已確認的 After 寫入 |
| S3 | E4 | 回歸（呼叫者 `skill-derive-template`、`skill-derive-script`） | 兩者委派 `skill-form-sop` 更新 SOP 的步驟 | 無 | 逐步演練：兩者都傳入已確認的 After；新條列只在 After 重述了新樣板或腳本的內容時才回報差異，其餘行為不變。本次修改沒有改變它們的委派內容，所以改為逐步演練 |
| S4 | E1、E4 | 回歸（呼叫者 `skill-engineering`） | 本次 04、05 的 W1 由 `skill-engineering` 委派新版 `skill-form-sop` 施工 | 無 | 施工時 `skill-form-sop` 讀取目標 Skill 的部位，並以新條列逐步檢查，沒有回報差異，寫入結果與 To-Be 逐字一致 |

## 8. 驗收結果

- 施工後結構診斷：`skill-form-sop` 0 個 error、0 個 warning；`SKILL.md` 與 To-Be 逐字一致；`rules/SOP-格式規範.md` 與 `W2-SOP-格式規範.md` 逐字一致。
- 連結同步結果：10 個 Skill 連結正常，沒有衝突或缺漏。
- 驗收方式：S1、S2 各自在包含本次未提交修改的暫存專案複本中，由不帶本次分析脈絡的子代理實際執行；測試用 Skill 建立在複本內，執行前先提交基準。
- S1 通過（E1–E3）：
  - 需求只要求新增寫入 `CHANGELOG.md` 的步驟。子代理在 Phase 1 讀取了目標 SOP 載入的 `rules/發行說明-格式規範.md`，並依新的 Rule 4 **主動刪除**步驟 3 重述的「每個項目必須以動詞開頭，並附上 PR 編號」（E1）。
  - 只屬於單一步驟的「檔名中的日期使用 `YYYY-MM-DD` 格式」保留（E2）。
  - 標題結構、受控動詞、完成條件與按需載入的檢查都通過；副作用只有目標檔案（E3）。
- S2 通過（E1、E2、E4；呼叫者 `skill-derive-rule`）：
  - 抽取後的 `WRITE` 步驟只引用「commit message 格式檢查清單」，沒有殘留「50 字元」「祈使句」（E1）；只屬於單一步驟的「不直接執行 `git commit`」保留（E2）。
  - `skill-form-sop` 讀取了新建的 RuleFile，依已確認的 After 寫入，沒有回報差異（E4）。
  - 子代理指出 `skill-derive-rule` 本身沒有規定要把步驟原內文移進 RuleFile，它是依新的 `SOP-格式規範` Rule 4 移除的。這驗證了 05 根因報告 H4（抽取後殘留原內文）這條路徑，現在已由 `skill-form-sop` 攔截。
- S3 通過（E4；呼叫者 `skill-derive-template`、`skill-derive-script`，逐步演練）：兩者的 Phase 3 步驟 4 都把已確認的 After 交給 `skill-form-sop`；新版 P1-1 會多讀取新建的樣板組或腳本介面，Rule 4 只會在 After 重述了樣板或腳本的內容時回報差異；委派內容與其他行為不變。
- S4 通過（E1、E4；呼叫者 `skill-engineering`）：施工 05 的 W1 時，新版 `skill-form-sop` 讀取 `skill-engineering` 的部位後，攔下已確認 To-Be 中 2 處步驟重述（P5-3、P2-4），依 P1-3 回報差異並停止；施工 04 的 W1 時，逐步檢查後沒有差異，寫入結果與 To-Be 逐字一致。
- 觀察（不影響通過）：子代理指出，當 After 會載入新建的 RuleFile 時，P1-1 的「其 SOP 載入的 RuleFile」可以解讀為修改前或修改後的 SOP；兩個子代理都選擇讀取修改後會載入的部位，結果正確。
- 真實專案副作用：沒有。

## 9. 第二輪修改（施工 07 時發現）

- 發現：`skill-form-sop` P1-3「將工作拆成必要的 Phase、步驟與完成條件」的「必要的」，重述 `SOP-格式規範` Rule 4「SOP 只保留必要步驟」的判準（與 04 的 P1-7「設計必要的類別」同一種情況）；P2-1「建立或更新唯一的 `# SOP` section」的「唯一的」，重述 `SOP-格式規範` Rule 1「每個 `SKILL.md` 必須有唯一一個 `# SOP` section」。
- 驗收條件：沿用 E1–E4。
- 確認紀錄：2026-09-25 使用者回覆「確認」

| 元素 | 動作 | 內容 | 追溯 |
| ---- | ---- | ---- | ---- |
| P1-3 | 修改 | 刪除「必要的」，其餘文字不變 | E1（C3：SOP 只保留必要步驟，存放在 `rules/SOP-格式規範.md` Rule 4） |
| P2-1 | 修改 | 刪除「唯一的」，其餘文字不變 | E1（C4：唯一一個 `# SOP` section，存放在 `rules/SOP-格式規範.md` Rule 1） |

| 順序 | 工作項 | 執行者 | 目標 | 內容 | 約束 | 狀態 |
| ---- | ------ | ------ | ---- | ---- | ---- | ---- |
| 3 | W3 | `skill-form-sop` | `skill-form-sop` 的 `SKILL.md` 中 Phase 1 -- 理解與設計拆解工作的 `THINK` 步驟、Phase 2 -- 寫入與驗證寫入 SOP 的 `WRITE` 步驟 | 刪除「必要的」「唯一的」；完成後與同目錄 `W3-SKILL.md` 逐字一致 | C3、C4 | 已完成 |

- 驗收：施工後以新版 `SOP-格式規範` Rule 4 逐步檢查 `skill-form-sop` 的 5 個步驟；結構診斷 0 error。

### 第二輪驗收結果

- W3 與 `W3-SKILL.md` 逐字一致；結構診斷 0 個 error、0 個 warning；以新版 `SOP-格式規範` Rule 4 逐步檢查 5 個步驟，沒有重述。
- 07-S2 中，新版 `skill-form-sop` 攔下 `skill-derive-rule` 的 After 把樣板段落順序留在步驟內文，驗證本關卡在 derive 路徑持續有效。
