# 根因報告：plan-with-class-diagram－重構優化

- 紀錄目錄：`specs/skill-engineering/08-plan-with-class-diagram-重構優化/`
- 問題證據：本次對話上下文（使用者要求「重構 plan-with-class-diagram，在不改變既有行為的前提下，找出可優化之處並且優化」）；目標 Skill 的 `SKILL.md`、兩份 RuleFile、類別圖提案樣板組與 `scripts/inspect_java_classes.py`；專案現行的 `skill-form-description`、`skill-form-sop`、`skill-form-rule`、`skill-form-template`、`skill-form-script` 規範；上一輪紀錄 `specs/skill-engineering/04-plan-with-class-diagram-規則重複存放/`；以 `markdown-it-py`（CommonMark＋table）與 `marked@12 --gfm` 渲染 `rules/類別圖輸出-格式規範.md` Rule 4 的結果；在 `java-web-framework` 執行腳本與 `ruff` 檢查的結果
- 確認紀錄：2026-09-25 使用者逐項詢問 R1–R4 的意義後，確認納入 R1（以 S4 驗證觸發結果不變）、同意 R2、R3、R4；同意推定的 E1 作為判斷標準；同意 H8 維持由 AI 決定計畫目錄編號、不改用腳本（使用者回覆「好，同意」）。第 8 節第 5 項的行為問題不納入本次修改

## 1. 使用者回報的問題

使用者沒有回報執行結果的偏差，而是要求在不改變既有行為的前提下找出可優化之處。本報告以專案現行的 skill-form-* 規範逐一稽核目標 Skill 的每個部位，把不符合規範、或渲染後會遺失內容的位置視為落差；會改變行為的問題只列為觀察，不納入本次修改。

## 2. 驗收條件

| 編號 | 驗收條件 | 來源 | 適用範圍 | 驗收情境 |
| ---- | -------- | ---- | -------- | -------- |
| E1 | 目標 Skill 的 frontmatter、SOP 與兩份 RuleFile 逐條通過 `skill-form-description`、`skill-form-sop`、`skill-form-rule` 的完成檢查清單；RuleFile 以 GFM 渲染後，每個 Mermaid 關係符號都完整顯示 | 推定（理由：使用者要求「找出可優化之處」但沒有定義判斷標準；本專案的 Skill 都以 skill-form-* 規範作為品質標準） | 通用 | S3 |
| E2 | 不改變既有行為：修改前後 E3–E10 都成立；計畫目錄的編號規則、提案檔結構、圖例、確認閘門與 Phase 轉移條件的意義都不變 | 本次 | 通用 | S1–S4 |
| E3 | 提案檔依序包含「類別圖」與「設計說明與實作順序」兩個頂層 Section，並寫入 `specs/plan/<NN>-<計畫名稱>/class-diagram-proposal.md` | 既有 | 通用 | S1 |
| E4 | 類別圖通過 Mermaid 語法檢查且不含 `classDef` 或 `style`；類別圖、設計說明與實作順序使用相同的類別名稱，描述的變更類型也一致 | 既有 | 通用 | S1 |
| E5 | 每個類別帶有一個圖例中的類別標記；`<<modified>>` 類別至少有一個帶標記的成員；成員與關係標記的位置符合圖例 | 既有 | 通用 | S1 |
| E6 | 實作順序把被依賴者排在依賴者之前，刪除步驟排在所有依賴者改寫之後 | 既有 | 通用 | S1 |
| E7 | 使用者明確確認提案前，不修改任何程式碼 | 既有 | 通用 | S1 |
| E8 | 使用者確認後，程式碼依已確認提案的實作順序實作並回報驗證結果；實際狀態需要改變類別圖時，返回 Phase 1 重新取得確認 | 既有 | 通用 | S2 |
| E9 | 需求涉及 Java 原始碼時，以類別結構腳本取得有上限的摘要，並檢視未解析引用與截斷狀態後再閱讀相關程式碼 | 既有 | 通用 | S1 |
| E10 | 觸發範圍不變：實作或重構前會新增或變更類別的請求使用本 Skill；不涉及類別新增或變更的請求不使用本 Skill | 既有 | 通用 | S4 |

驗收情境：

| 編號 | 驗證 | 類型 | 輸入 | 預先回覆 | 判斷方式 |
| ---- | ---- | ---- | ---- | -------- | -------- |
| S1 | E2–E7、E9 | 回歸 | 包含本次修改的專案複本，需求：為 `java-web-framework` 加入 middleware 鏈，可在 Handler 執行前後處理請求，並可以提前回應 | Phase 2 回覆「先不要實作」 | E3：提案檔位於 `specs/plan/04-<名稱>/class-diagram-proposal.md`，兩個頂層 Section 依序出現；E4：以 `mermaid@10.2.3` 解析通過，沒有 `classDef`／`style`，圖文名稱與變更類型一致；E5：每個類別恰有一個圖例中的類別標記，`<<modified>>` 類別至少有一個帶標記的成員；E6：被依賴者先於依賴者；E7：`git status` 只有新增的提案目錄；E9：子代理回報腳本命令與截斷、未解析引用的檢視結果 |
| S2 | E2、E8 | 回歸 | 包含本次修改的專案複本，需求：讓 `WebApplication` 可以註冊自訂的 404 Not Found 回應 | Phase 2 回覆「確認」 | 程式碼修改的順序與已確認提案的實作順序一致；實作的類別組織符合類別圖；回報 `mvn test` 結果 |
| S3 | E1、E2 | 本次 | 施工後的目標 Skill | 無 | 依三個 skill-form-* 的完成檢查清單逐條檢查，全部通過；以 GFM 渲染 `rules/類別圖輸出-格式規範.md`，六種關係的符號都完整出現；逐段比對修改前後的 diff，沒有任何行為約束的意義改變 |
| S4 | E2、E10 | 回歸 | 專案所有 skills 目錄中各 Skill 的 `description`，分別搭配修改前與修改後的目標 Skill `description`；六個請求：「幫 java-web-framework 加 middleware，先規劃類別再實作」「重構 Router，把路由比對抽成獨立類別」「Add a caching layer to UserService and plan the classes first」「修正 Route.java 註解的錯字」「說明 IoC 容器的原理」「幫我寫這次變更的 commit message」 | 無 | 不帶本次脈絡的子代理只依 `description` 判斷每個請求應使用的 Skill；修改前後的判斷結果相同，前三個使用本 Skill，後三個不使用 |

## 3. 落差

| 驗收條件 | 預期 | 實際 | 證據 |
| -------- | ---- | ---- | ---- |
| E1 | `description` 使用 Skill 的主要語言，並以「時使用」或「必須使用此 Skill」表達觸發情境與關鍵字 | `description` 為英文，只描述流程結果，沒有明確的觸發用詞與中文關鍵字 | 觀察：`SKILL.md` 第 3 行；SOP 與所有部位都是中文；`skill-form-description` `Skill描述-格式規範` Rule 2 第 2–4 條 |
| E1 | 每個步驟只描述一個具體行為 | P1-7 同時設計類別與實作順序，又決定計畫名稱與編號；P1-8 只驗證前者 | 觀察：`SKILL.md` P1-7、P1-8；`skill-form-sop` `SOP-格式規範` Rule 2 第 2 條；`skill-engineering` `施工委派-格式規範` Rule 2 第 3 條以「類別設計與計畫編號」作為不同動作的例子 |
| E1 | Good 與 Bad Example 針對同一個規則重點與情境 | `類別設計` Rule 4 的 Good Example 示範排序與刪除，Bad Example 示範循環相依；循環沒有正例，排序沒有反例 | 觀察：`rules/類別設計-格式規範.md` Rule 4；`skill-form-rule` `RuleFile-格式規範` Rule 9 第 5 條 |
| E1 | 渲染後每個 Mermaid 關係符號完整顯示 | 表格中「繼承」與「Interface 實作」兩列的符號只剩 `` `< ``，其餘內容被切到不存在的欄位 | 觀察：`rules/類別圖輸出-格式規範.md` Rule 4 表格；兩個 GFM 解析器的輸出都是 `<td>`&lt;</td>`；GFM 規格：表格中的 `|` 即使位於 code span 內也會分隔儲存格 |

## 4. 演練表

本次沒有執行結果的偏差，因此演練比對每個步驟與部位是否符合現行 skill-form-* 規範。

`plan-with-class-diagram`：

| 步驟 | 應載入 | 實際載入 | 應產出 | 實際產出 | 判定 |
| ---- | ------ | -------- | ------ | -------- | ---- |
| frontmatter | 無 | 無 | 以主要語言寫明用途、觸發情境與關鍵字的 `description` | 觀察：英文、沒有觸發用詞與關鍵字 | 偏離 |
| P1-1～P1-5 | 腳本 | 觀察：P1-3 委派 | 專案輸入、類別結構摘要、相關程式碼現況 | 觀察：各步驟一個行為，只含控制平面 | 一致 |
| P1-6 | 無 | 無 | 類別設計檢查清單 | 觀察：載入 `類別設計` Rule 1–4 | 一致 |
| P1-6 載入的 `類別設計` Rule 4 | — | — | 同一情境的正反範例 | 觀察：正例與反例示範不同重點 | 偏離 |
| P1-7 | 類別設計檢查清單 | 觀察：已使用 | 類別設計與實作順序 | 觀察：另含計畫名稱與編號的決定 | 偏離 |
| P1-8 | 類別設計檢查清單 | 觀察：已使用 | 驗證結果 | 觀察：只引用檢查清單 | 一致 |
| P1-9 | 無 | 無 | 類別圖輸出檢查清單 | 觀察：載入 `類別圖輸出` Rule 1–6 | 一致 |
| P1-9 載入的 `類別圖輸出` Rule 4 | — | — | 渲染後可讀的符號對應 | 觀察：兩列符號渲染後遺失 | 偏離 |
| P1-10～P1-12 | 類別圖輸出檢查清單、類別圖提案樣板組 | 觀察：已讀取 | 提案檔與驗證結果 | 觀察：只引用載入結果與寫入路徑 | 一致 |
| P2-1、P2-2 | 無 | 無 | 展示與確認閘門 | 觀察：只含流程 | 一致 |
| P3-1～P3-4 | 無 | 無 | 程式碼與驗證結果 | 觀察：各步驟一個行為，引用已確認提案 | 一致 |
| 類別圖提案樣板組 | — | — | 符合 `樣板組-格式規範` Rule 1–5 | 觀察：雙檔同名、填位符號一致、範例無殘留填位符號 | 一致 |
| `scripts/inspect_java_classes.py` | — | — | 符合 `Python腳本-格式規範` Rule 1–6 | 觀察：PEP 723、`argparse`、`Path`、非零退出與標準錯誤輸出都符合；在 `java-web-framework` 以 0.05 秒完成 | 一致 |

第一個偏離點：frontmatter。之後的 P1-7、`類別設計` Rule 4、`類別圖輸出` Rule 4 是各自獨立的偏離點，沒有前後因果關係。

## 5. 根因

| 編號 | 位置 | 缺陷類型 | 說明與證據 | 反事實檢驗 |
| ---- | ---- | -------- | ---------- | ---------- |
| R1 | `plan-with-class-diagram`／`SKILL.md`／frontmatter `description` | 觸發缺陷 | 觀察：`description` 為英文，沒有「時使用」等觸發用詞，也沒有使用者會提到的關鍵字。推論：中文請求只能靠跨語言的語意比對觸發（依據前項觀察與 `Skill描述-格式規範` Rule 2）。目前沒有觀察到誤觸發或漏觸發 | 改為中文、保留原本的觸發範圍與「先確認再實作」的關鍵行為後，S4 的六個請求判斷結果與修改前相同，E10 仍然滿足 |
| R2 | 同檔 SOP／Phase 1 -- 分析與提案／P1-7 | 流程缺陷 | 觀察：P1-7 在同一步驟完成兩個行為，P1-8 只驗證其中的類別設計 | 把計畫名稱與編號拆成 P1-8 之後的獨立 `THINK` 步驟、文字保留原文後，兩項決定的內容與順序不變，E3、E6 仍然滿足 |
| R3 | `rules/類別設計-格式規範.md`／Rule 4 範例 | 缺失 | 觀察：Rule 4 的條列同時規範排序與循環相依，但排序只有正例、循環只有反例 | 補成兩組同情境的正反範例後，條列不變，E6 的判斷依據不變 |
| R4 | `rules/類別圖輸出-格式規範.md`／Rule 4 符號對應表 | 不可檢查 | 觀察：渲染後「繼承」與「Interface 實作」的符號遺失，只讀渲染結果的人無法用這張表檢查這兩種符號。Agent 讀取的原始文字正確，因此不影響目前的產出 | 把表格改寫為條列、對應內容逐字不變後，原始文字與渲染結果一致，E4 的判斷依據不變 |

## 6. 被排除的假設

| 假設 | 缺陷類型 | 排除依據 |
| ---- | -------- | -------- |
| H5：`類別圖輸出` Rule 6 第 2 條重述圖例的標記意義 | 衝突 | 觀察：圖例只定義標記的意義，第 2 條規定判定依據是「相關程式碼現況」，並把這個依據寫成可檢查的兩項判準；這兩項來自 03 紀錄中使用者確認的 E1 |
| H6：`類別設計` Rule 2 第 4 條與 Rule 1 第 3 條重複 | 衝突 | 觀察：Rule 1 禁止任何只為未確認需求存在的類別；Rule 2 第 4 條提供判斷 Interface 與 abstract class 的具體條件（替換、隔離、共用契約），判準不同 |
| H7：腳本需要重構 | 載體過弱 | 觀察：`ruff`（F、B、SIM、PL）只回報 `resolve_reference` 有 9 個位置參數；在 `java-web-framework` 執行 0.05 秒，輸出 8 個類別、未截斷，與 04 S1 一致；參數數量不影響 Skill 的任何產出 |
| H8：P1-7 的計畫編號應改由腳本產生 | 載體過弱 | 觀察：03 S1、04 S1 與 S2 的計畫編號都正確，沒有結果不穩定的證據；改用腳本需要新增 1 個腳本與 2 個步驟 |
| H9：P1-10 的寫法與 P1-6、P1-9 不一致 | 衝突 | 觀察：`SOP-按需載入-格式規範` Rule 1 只要求「特定條件下才需要」的參考檔說明適用條件，樣板組每次都需要；沒有違反規範，依 `重新設計-格式規範` Rule 1 保留原文 |
| H10：Phase 3 重複使用「已確認的 Markdown 類別圖提案檔」全名 | 衝突 | 觀察：`SOP-格式規範` Rule 5 只要求輸入名稱可辨識，全名符合；沒有違反規範 |
| H11：使用者個案偏好 | 非 Skill 問題 | 觀察：R1–R4 違反的是專案所有 Skill 共用的 skill-form-* 規範，屬於通用落差 |

## 7. 影響範圍

無：沒有其他 Skill 委派 `plan-with-class-diagram`。

## 8. 待確認事項

1. 第 2 節的驗收條件是否正確、完整？E1 是推定的判斷標準（以專案現行的 skill-form-* 規範作為「可優化」的依據），是否同意？
2. 是否同意第 5 節的根因 R1–R4？
3. R1 是唯一會碰到觸發判斷的項目：改寫後的觸發範圍保持不變，並以 S4 驗證修改前後判斷相同。是否納入本次修改？
4. H8：計畫編號改由腳本產生，目前沒有不穩定的證據，建議不做。是否同意？
5. 下列問題會改變既有行為，不納入本次修改，只記錄下來，之後由你決定是否另外優化：
   - Phase 2 要求修改而返回 Phase 1 時，P1-7 會把剛建立的計畫目錄算進既有編號，修訂版因此寫入新的 `<NN+1>` 目錄，而不是更新原本的提案檔。
   - P3-4 執行測試或 lint 失敗時，SOP 沒有規定要修正後重跑，還是只回報結果。
   - 04 紀錄留下的觀察：P1-3 的 `--project-root` 應該傳 Java 模組目錄還是專案根目錄；腳本不追蹤方法本體內的呼叫，`truncated: false` 容易被誤讀為摘要完整；設計說明中的示範程式碼出現圖外類別時，`類別圖輸出` Rule 5 是否適用；需求已經滿足時的處理方式。
